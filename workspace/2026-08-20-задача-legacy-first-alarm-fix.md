# Задача (зафиксировано дословно)

Владелец, 2026-08-20, диалог про 5 претензий «руководителя» к работе Engineer (несколько сообщений подряд, сведено без причёсывания формулировок):

1. «так и после того как он тебе это сказал чтобы было дальше?» — что Engineer сделает, чтобы такие случаи не повторялись при построении агентов.
2. Engineer предложил: (а) дописать третий задокументированный случай пропуска Legacy-first Alarm в `capability-creation-methodology`, той же секцией, что уже ведёт счёт повторам; (б) сохранить это как memory для себя.
3. Владелец: **«внеси се изменения — затем перепроверь вместе с хайку ( он вообще подключается) — покажи своему начальнику»**.

# Критерий достаточности (зафиксирован Engineer до вызова Reviewer)

«Все изменения внесены» означает конкретно:

1. В `ENGINEER/skills/capability-creation-methodology/SKILL.md`, секция «Фаза 2 — Legacy-first Alarm», добавлен текст, который: (а) описывает третий повтор того же класса ошибки, дата 2026-08-20, инцидент — Positioning Statement `AI Marketing Strategist`; (б) явно формулирует, что у Legacy-first Alarm нет исключения «это же просто тест» — правило действует, даже если действие названо тестом/проверкой.
2. Существует новый файл в `C:\Users\bogol\.claude\projects\C--Users-bogol-OneDrive--------------ENGINEER\memory\` с frontmatter `type: feedback`, описывающий то же правило (rule → Why → How to apply), не дублирующий по смыслу уже существующие memory-файлы.
3. `MEMORY.md` (в той же memory-директории) содержит одну новую строку-указатель на этот файл, в формате, совпадающем с остальными строками индекса (`- [Title](file.md) — hook`), без содержания memory прямо в индексе.
4. Ничего из существующего содержимого `capability-creation-methodology/SKILL.md` и `MEMORY.md` не удалено и не переписано — только добавлено.

Reviewer проверяет соответствие этим 4 пунктам по факту содержимого файлов — не спрашивает Engineer, что имелось в виду.
