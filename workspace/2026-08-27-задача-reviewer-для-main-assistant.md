# Задача — независимая проверка (reviewer) перед значимым ответом MAIN_ASSISTANT

Дословно, из сессии 2026-08-26 → 2026-08-27 (диалог с владельцем, ENGINEER):

Владелец, после разбора реального кейса (документ про Авито, 280 КБ) и находки Researcher
(пять точечных правил в HOME.md — это класс исправления, для которого есть прямое
эмпирическое основание не считать его надёжным: самопроверка не работает, если её выполняет
тот же процесс, что произвёл ответ; работает структурно отделённая проверка — другой актор
или отдельная точка потока с правом veto):

«строй проверяющего»

Контекст решения (та же сессия, чуть раньше): владелец прямо спросила про сам механизм —
«какие у тебя предложения???» — предложила независимого проверяющего по образцу уже
рабочего механизма ENGINEER (`.claude/agents/reviewer.md`), не Plan-режим (та идея отдельно,
менее проверена).

## Критерий достаточности (зафиксирован до работы)

У MAIN_ASSISTANT уже есть `.claude/agents/reviewer.md`, поставлен 2026-08-19 для других
значимых шагов (Rule Promotion L3+, Agent Specification, Engineering Hand-off) — по
`instructions/capability-resolver.md` расширяю существующий механизм на новый триггер, не
строю параллельный.

Готово, если:
1. В `HOME.md` MAIN_ASSISTANT добавлен явный шаг: перед содержательным анализом или
   переходом к делегированию — вызвать reviewer с TASK/CRITERION/RESULT.
2. CRITERION — конкретный, проверяемый список, привязанный к реальным сегодняшним находкам
   (потеря цифр, делегирование как способ закрыть вопрос раньше времени, инвентаризация
   вместо разбора по существу, недоверенные данные без пометки) — не абстрактное «думай
   лучше».
3. Честно названо ограничение: вызов reviewer всё ещё держится на том, что MAIN_ASSISTANT
   сам не забудет его вызвать — сама проверка отделена, решение вызвать её — нет. Не
   выдаётся за полное структурное решение (Plan-режим), это отдельный, менее проверенный
   вариант, не сделан в этот же заход.
4. Статус в `DECISIONS.md` — не `Locked`, первый реальный прогон ещё не был.
