# 2026-09-02 — Задача: внести стандарт Data Contract / запрет ad-hoc генерации / E2E-приёмку в repository-design, синхронизировать PROFILE.md, зафиксировать в DECISIONS.md

## Слова владельца (дословно)

«Принимаем выводы аудита Codex и объединяем их в единый стандарт ENGINEER.
Внеси следующие изменения в файлы директории ENGINEER:

1. ОБНОВЛЕНИЕ ЧЕК-ЛИСТА СБОРКИ (repository-design/SKILL.md):
   - Добавь обязательный пункт: Data Contract (Pydantic-модель или строгая JSON Schema с перечнем обязательных полей) ДО написания бизнес-логики агента.
   - Введи запрет на ad-hoc скриптинг генерации документов: жесткий стандарт — только библиотека промышленных шаблонов (docxtpl/Jinja2) с чистыми метками {{ variable }}.
   - Добавь механизм принуждения для эталона качества: агент не может быть сдан без зафиксированного файла-эталона с датой ДО первого рабочего коммита.

2. КРИТЕРИИ ПРИЁМКИ (End-to-End Acceptance):
   - Переопредели «реальные данные»: запрещено считать приёмкой прогон на файлах из examples/.
   - Приёмка считается пройденной ТОЛЬКО на «грязном» реальном пользовательском кейсе (с пропусками данных, где агент обязан остановиться и задать вопросы через опросник-интервьюер).

3. ГИГИЕНА И СИНХРОНИЗАЦИЯ:
   - Синхронизируй PROFILE.md с фактами из MEMORY.md (устрани разрыв).
   - Удали пустые/лишние файлы-заглушки (.gitkeep в непустых папках).
   - Зафиксируй в DECISIONS.md принятие стандарта: «Архитектурный стандарт сборки агентов: Data Contracts, эталонные шаблонизаторы и E2E-приёмка на реальных кейсах» со статусом Locked.

После внесения изменений покажи краткий git diff или отчёт о правках.»

Прямое продолжение уже показанного и обсуждённого независимого аудита Codex по ENGINEER
(`workspace/2026-09-02-задача-фиксация-6-правил-юрист-и-аудит-engineer.md`) и сравнения
его выводов с черновиком владельца «4 правила Архитектора бизнес-решений» — сравнение
показано, одобрение на внедрение получено явно этим сообщением.

## Критерий достаточности (зафиксирован до работы)

1. `skills/repository-design/SKILL.md` содержит: обязательный Data Contract до бизнес-логики,
   запрет ad-hoc генерации документов в пользу промышленного шаблонизатора, усиленный
   механизм принуждения для эталона Researcher (файл с датой до первого коммита),
   переопределённое понятие «реальные данные» (без `examples/`), явный критерий
   E2E-приёмки на «грязном» кейсе с обязательным опросником при пропусках.
2. `PROFILE.md` заполнен фактами, уже подтверждёнными в `MEMORY.md`/этой же сессии —
   не выдуман, не выходит за рамки того, что уже известно и подтверждено словами владельца.
3. `workspace/.gitkeep` и `memory/.gitkeep` удалены (папки не пусты — подтверждено перед
   удалением, не по памяти аудита).
4. `DECISIONS.md` — новая запись `Locked`: «Архитектурный стандарт сборки агентов: Data
   Contracts, эталонные шаблонизаторы и E2E-приёмка на реальных кейсах».
5. Отчёт владельцу — сжатый diff/список правок по каждому файлу.
