# 2026-09-03 — Задача: Волна 1 (портируемость MCP + изоляция клиентских документов Юриста)

## Слова владельца (дословно)

«Даю добро на исполнение Волны 1 (быстрые победы):

1. Портируемость MCP (Блок 3):
   - Создай локальные .mcp.json для ENGINEER и Marketing Strategist по проверенному шаблону
     (с плейсхолдерами ${EXA_API_KEY} и ${FIRECRAWL_API_KEY}).
   - Убедись, что реальные ключи не попадают в git (добавь .env / .env.local в .gitignore
     каждого репозитория при необходимости).

2. Изоляция Legal Constructor (Блок 1.2):
   - Создай структуру data/clients/ и перенеси туда клиентские файлы (*.docx, *.doc, папки
     ЛИЧНОЕ/, Оригинал/) из корня.
   - Добавь data/clients/*/ в .gitignore (с сохранением README.md).
   - Внеси краткую поправку в HOME.md о новом пути клиентских документов и исключении
     data/clients/ из широкого поиска по умолчанию.

По завершении покажи git status / краткий git diff по обоим направлениям.
К Волне 2 (архивация DECISIONS.md) пока не переходи.»

## Критерий достаточности (зафиксирован до работы)

1. `.mcp.json` создан у ENGINEER и Marketing Strategist — без реальных ключей, только
   `${ENV_VAR}`-плейсхолдеры; `.env`/`.env.local` в `.gitignore` у обоих (уже были).
2. У Legal Constructor: `data/clients/<слаг-дела>/` создана, найденные в корне клиентские
   файлы перенесены туда; `data/clients/*/` в `.gitignore`, `README.md` сохранён; `HOME.md`
   получает короткий пункт про новый путь и исключение из широкого поиска по умолчанию.
3. Волна 2 (архивация DECISIONS.md) не начинается этим ходом.
4. По завершении — `git status`/diff по каждому репозиторию.
