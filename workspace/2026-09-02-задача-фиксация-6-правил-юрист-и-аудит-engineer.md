# 2026-09-02 — Задача: внести 6 правил Codex в устав Юриста, убрать утёкший токен, запустить аудит ENGINEER

## Слова владельца (дословно)

«Отлично. Анализ Codex полностью принят — это именно те механизмы, которых не хватало.

Финализируем Юриста и сразу переходим к корню проблемы:

1. ПО ЮРИСТУ:
   - Внеси эти 6 правил Codex в инструкции Юриста (SOUL.md / HOME.md / регламент сборки документов).
   - Удали telegram_bot/bot.log и внеси *.log в .gitignore.
   - Отчитайся о фиксации.

2. СЛЕДУЮЩИЙ ШАГ — АУДИТ ENGINEER:
   - Сразу после фиксации Юриста переходи к аудиту папки ENGINEER через Codex CLI по стандарту instructions/codex_deep_audit_prompt.md.
   - Цель аудита Инженера: вскрыть идейные и архитектурные дефекты того, как ты проектируешь агентов (почему допускается ad-hoc скриптинг вместо готовых шаблонизаторов, почему нет обязательных схем валидации данных и почему приёмка идёт по отсутствию ошибок в консоли, а не по сквозным бизнес-сценариям).»

Контекст: шесть правил — прямой вывод независимого аудита Codex 2026-09-02 (полный аудит
`AI Legal Constructor` + прицельный анализ обезличенной реальной переписки по сделке
аренды), уже показанный и обсуждённый с владельцем в этой же сессии. Механизм
двухконтурного аудита через Codex зафиксирован `Locked` в `ENGINEER/DECISIONS.md`
(«Стандартизация независимого аудита агентов через Codex»).

## Критерий достаточности (зафиксирован до работы)

**По Юристу:**
1. Шесть правил Codex внесены в файлы `AI Legal Constructor` (`HOME.md` — диалоговый флоу;
   `skills/document-assembly/SKILL.md` — механика сборки), с явной ссылкой на источник,
   не растворены в общих словах.
2. `telegram_bot/bot.log` (содержал реальный токен Telegram) удалён физически.
3. `.gitignore` Юриста проверен на `*.log`/`telegram_bot/bot.log` — не задваивать, если
   уже есть.
4. Отчёт владельцу — что внесено, что уже было готово заранее.

**По аудиту ENGINEER:**
5. Полный сквозной аудит `ENGINEER` через Codex CLI по протоколу
   `instructions/codex_deep_audit_prompt.md`, с дополнительным фокусом, заданным
   владельцем: почему допускается ad-hoc скриптинг вместо готовых шаблонизаторов,
   почему нет обязательных схем валидации данных, почему приёмка идёт по отсутствию
   ошибок в консоли, а не по сквозным бизнес-сценариям.
6. Секреты (если найдутся в ENGINEER) изолируются на время аудита — тот же протокол,
   что уже применялся к `AI Avitolog` и `AI Legal Constructor`.
7. Результат — тот же формат: инвентаризация, критические блокеры Craft/Engineering,
   кандидаты на удаление, итоговый вердикт READY/ACTION REQUIRED.
