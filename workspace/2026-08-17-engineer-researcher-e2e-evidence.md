# Engineer ↔ Researcher — E2E evidence (после правки MISSION.md/METHODOLOGY.md/COMMUNICATION.md)

Дата: 2026-08-17
Что проверяли: реальный контракт Engineer↔Researcher на практической задаче — что передаётся, что реально получает Researcher, что возвращает, содержит ли результат evidence+аналитический вывод без приоритета/архитектуры/места внедрения, использует ли Engineer результат дальше. Проверка по execution trace, не по самоотчёту, где trace был доступен.

Метод: реальный независимый процесс (`claude -p`, cwd = `ENGINEER`, отдельный от текущей сессии), которому передана роль Engineer с инструкцией следовать `ENGINEER/COMMUNICATION.md`. Задача — реальная, не искусственная: подобрать проверенные паттерны предотвращения рассинхрона статуса между независимыми markdown/git-репозиториями (прямое продолжение находки этой же сессии — конфликт Locked/Proposal между `ENGINEER` и `AI Intelligence`).

Session ID (внешний процесс, «Engineer»): `102d7747-738c-4288-b16f-f320a475eefb`. Trace: `~/.claude/projects/C--Users-bogol-OneDrive--------------ENGINEER/102d7747-738c-4288-b16f-f320a475eefb.jsonl`.

## 1. Что Engineer передаёт Researcher

**FACT**, извлечено напрямую из trace (строка 20, реальный payload вызова `Agent`, не пересказ): контракт из трёх шагов — (1) обязательно первым прочитать устав по цепочке `AI Intelligence/CLAUDE.md → HOME.md → MISSION.md`, (2) контекст задачи, (3) узкий research brief с критерием достаточности, зафиксированным Engineer заранее и явно переданным Researcher как часть брифа, с явным перечислением того, что критерий **исключает** (приоритет, место внедрения, архитектурное решение). Отдельно — инструкция сообщить прямо, если устав Researcher противоречит брифу, не молчать об этом.

## 2. Что Researcher реально получает

**FACT** — это тот же payload, что в п.1: сама API-нагрузка вызова, не отчёт о том, что должно было быть передано.

## 3. Что Researcher возвращает

**FACT**, перепроверено по двум независимым источникам — ответу во внешнем процессе и реально сохранённым файлам в `AI Intelligence` (не только текст ответа):
- `knowledge/evidence/2026-08-17-cross-repo-status-sync-patterns.md` (новый файл) — 3 паттерна (git submodule, ADR-supersession + централизация Richards & Ford, shields.io badge), каждый с прямым источником.
- `knowledge/EVIDENCE_INDEX.md` — новый `Batch SYNC`, с прямой cross-reference-строкой на файл выше (тот же паттерн, который мы только что сами внесли для `Batch EXECREC` — применён Researcher самостоятельно, без напоминания).
- `knowledge/CAPABILITY_MAP.md` — обновлён (раздел Refinement).
- `OPEN_QUESTIONS.md` — новая запись, статус «Закрыт», с критерием и честно зафиксированным пробелом (паттерн TC39 proposals не засчитан — механизм не подтверждён).
- `memory/2026-08-17.md` — дневник.

## 4. Evidence + аналитический вывод о применимости — есть

**FACT.** Каждый из 3 паттернов в сохранённом файле имеет: источник, механизм, **явный вывод о применимости** (использовать полностью / адаптировать / отвергнуть) с обоснованием. Пример (Pattern 3, shields.io): вывод «отвергнуть», с честным обоснованием почему — не молчаливый пропуск, отказ так же полноценен, как принятие.

## 5. Не принимает ли Researcher решения о приоритете/архитектуре/месте внедрения

**FACT — граница соблюдена, причём явно, не по умолчанию.** Сам сохранённый файл заканчивается разделом «Explicitly not answered here»: «No verdict is given on priority between these three patterns, on where in `ENGINEER`/`AI Intelligence` to implement anything, or on which architecture to actually adopt — that is Engineer's/the owner's decision». Это не вывод из отсутствия упоминания — это прямое утверждение в файле, отражающее только что применённую правку `MISSION.md`.

## 6. Использует ли Engineer результат дальше

**FACT.** Внешний процесс явно оценил результат по критерию («выполнен с запасом»), затем сформулировал два конкретных следующих шага, опираясь на находки Researcher (указатель на коммит вместо текстового статуса; одно каноническое место для межрепозиторийных решений) — и сам явно отказался выбирать конкретное место/файл: «Это моя рекомендация, не решение — конкретное место внедрения... я не выбираю сейчас, это стоит обсудить». То есть даже Engineer в этом прогоне сама держит ту же границу, что мы только что зафиксировали для Researcher.

## UNKNOWN — честно, не скрываю

Не удалось подтвердить по trace, действительно ли субагент (Researcher) физически выполнил `Read` по `CLAUDE.md`/`HOME.md`/`MISSION.md` (шаг 1 контракта) — файл собственного execution trace субагента (`.../tasks/a3984c130f6f10206.output`) оказался пустым (0 байт) на момент проверки, вероятно потому что процесс уже завершился к моменту чтения. Утверждение «прочитала устав» в тексте ответа — самоотчёт, не подтверждённый независимо trace'ом. Это не опровержение, а честно неподтверждённый пункт — не выдаю самоотчёт за факт.

## Отклонения от контракта

Не найдено ни одного. Единственная зафиксированная самой Researcher процедурная тонкость — вопрос пришёл прямым вызовом, а не был предварительно записан в её `OPEN_QUESTIONS.md` до поиска (обычная для неё дисциплина «критерий до поиска» в этом случае выполнена самим Engineer до вызова, не нарушена, только процедурно иначе оформлена) — Researcher сама явно это отметила в записи, не скрыла.

## Итог

Контракт Engineer↔Researcher после правки `MISSION.md`/`METHODOLOGY.md`/`COMMUNICATION.md` работает на практике именно так, как теперь написан: intelligence + аналитический вывод о применимости — да; приоритет/архитектура/место внедрения — нет, ни у Researcher, ни (в этом прогоне) у самой Engineer. Единственный непроверенный пункт — фактическое чтение устава субагентом — технически недоказуем в этот раз из-за пустого trace-файла, не из-за найденного нарушения.
