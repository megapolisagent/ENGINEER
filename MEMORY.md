# Постоянная память ENGINEER

Что верно всегда. Пополняется одной строкой за раз, когда появляется новый постоянный факт — см. `.claude/rules/memory-rules.md`.

- Есть ещё одна память — встроенная авто-память среды выполнения (`~/.claude/projects/.../memory/`). Это не эта система: правила `.claude/rules/memory-rules.md` прямо говорят вести записи только сюда, в файлы этого репозитория. Не путать одну с другой при чтении на старте сессии.
