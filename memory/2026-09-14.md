# Дневник 2026-09-14

## Найдено и устранено самопротиворечие во Входном гейте, унаследованное от FOUNDATION и растиражированное минимум в двух агентах

### Что сделали
- По итогам внешнего разбора архитектуры `AI Marketing Strategist` (сессия владельца вне ENGINEER) найдено: `CLAUDE.md` §7 того агента и `CLAUDE.md` §3 этого агента содержат один и тот же кусок текста из `FOUNDATION/instructions/confirmation-before-action.md`, но при переносе он слился с осевым гейтом `HOME.md` («если оси низкие — действуй сразу») в одном абзаце — получилось самопротиворечие: «без исключений по осям» и «оси низкие → сразу» в соседних предложениях. Один и тот же текст оправдывал и избыточную интерогацию на рутинных шагах, и самовольный запуск субагента без подтверждения.
- Также найдено: `confirmation-before-action.md` добавлен в FOUNDATION в тот же день без записи в `DECISIONS.md` — нарушение собственной дисциплины журнала.
- Исправлено на источнике: `FOUNDATION/instructions/confirmation-before-action.md` откалиброван — гейт обязателен только для того, что уходит наружу или необратимо; внутреннее исследование в рамках одобренного скоупа из-под него выведено явно. `FOUNDATION/HOME.md` дополнен перекрёстной ссылкой, чтобы два гейта не сливались повторно при следующем переносе. `FOUNDATION/DECISIONS.md` — заведена пропущенная запись задним числом.
- Синхронизировано: `ENGINEER/CLAUDE.md` §3 переписан на два явно разделённых гейта (Confirmation Gate / Execution Gate). `AI Marketing Strategist/CLAUDE.md` §7 (уже содержал независимо придуманную, но по сути ту же калибровку с вчерашней сессии) — сведён к единой формулировке канона, добавлена перекрёстная ссылка на решение FOUNDATION.

### Что сделали (продолжение — тот же день, найден и закрыт корневой процессный дефект)
- Внешняя сессия проверила `check_skill_composability.py` (Composability & Tooling Gate) на `AI Marketing Strategist` — 6 из 12 скиллов флагованы (calc-сигналы в прозе без `tools/`), при том что тот же донорский скилл `humanizer` у `AI Avitolog` получил реальный `tools/check_banned_patterns.py`, а у CMO остался нетронутой архивной копией донора. Формулировка «гейт применяется и к уже существующим скиллам» существовала в `02-execution-gates.md` несколько недель, но ни разу не была реально прогнана против CMO — обнаружено не Engineer, а внешней сессией.
- Прогнан `check_skill_composability.py` на всех пяти известных агентах (ENGINEER, AI Marketing Strategist, AI Avitolog, AI Legal Constructor, AI Intelligence) — реальный baseline зафиксирован.
- **Корневой фикс, не разовая починка**: заведён `knowledge/agent-registry.md` — единственный источник правды по статусу переноса канона (Composability Gate, Confirmation Gate, hooks, data-discipline.md) на каждого известного агента, с датой. `02-execution-gates.md` дополнен: Composability & Tooling Gate и Canon Sync Gate теперь явно требуют записи в реестр как единственное доказательство выполнения пункта «применяется и к существующим тоже»; добавлен новый раздел Propagation Sweep — при появлении нового канонического правила перенос не считается завершённым, пока не пройдены ВСЕ строки реестра. `CLAUDE.md` §2 сослан на реестр как источник правды.
- Замечено и исправлено в процессе: собственная правка `02-execution-gates.md` изначально содержала нарративный маркер «Реальный случай (дата)», который `guard-rules-bloat.sh` обязан блокировать в `.claude/rules/` — переписано в голый алгоритм до сохранения, история перенесена в `knowledge/agent-registry.md` (не подпадает под запрет — не `.claude/rules/`).

### Что осталось
- Конкретные флаги из baseline-прогона (6 у CMO, 4 у Avitolog, 1 у Legal Constructor, 1 у AI Intelligence) не устранены — реестр их зафиксировал, но починка скиллов по этим флагам решение владельца, не выполнена этим ходом.
- `guard-rules-bloat.sh`/`block-dangerous-git.sh`/консолидация `data-discipline.md` перенесены на `AI Marketing Strategist` и `AI Avitolog` (внешней сессией), но НЕ на `AI Legal Constructor` и `AI Intelligence` — зафиксировано в реестре как `❌ не перенесено`, следующий Propagation Sweep обязан это закрыть.
- Входной гейт `AI Intelligence` не проверен на тот же самопротиворечивый паттерн — строка реестра пустая, не «чисто».

### Решения
- Confirmation Gate и Execution Gate — два разных, явно разделённых правила, не один абзац: первый безусловен по категории действия (внешнее/необратимое), второй калибрует глубину подготовки внутри уже разрешённой задачи. Решение владельца, 2026-09-14, зафиксировано в `FOUNDATION/DECISIONS.md`.
- Propagation отныне проверяется по `knowledge/agent-registry.md`, не по памяти Engineer о собственных намерениях — решение владельца, 2026-09-14, по прямому запросу «улучшить работу Инженера насовсем, чтобы не допускал таких грубых ошибок».

## Довершено тем же днём — все хвосты из первой записи закрыты, Propagation Sweep пройден целиком

### Что сделали
- Разобраны лично все 12 флагов baseline-прогона Composability Gate (не по строке реестра — каждый `SKILL.md` прочитан целиком). Найден и исправлен системный баг самого чекера `check_skill_composability.py`: регулярка Правила 2 матчила русский корень «формулировать/формулировка» как «формула» (расчёт) — общий префикс, разный смысл. Регулярка исправлена (`формул(?!ир)[а-я]*`), baseline пересчитан на всех пяти агентах.
- Итог разбора: 11 из 12 флагов — ложное срабатывание (8 из-за бага регулярки, 3 — риторическое/advisory использование слов «percentage»/«Calculate»/«формула», один случай — `last30days-skill` — скилл прямо запрещает пересчитывать то, что уже посчитал внешний движок). Единственный реальный — `avito-listing-packaging` (Правило 3, не 2): Модуль 2/5 не были разбиты вместе с Модулями 1/3/4 (2026-09-10).
- С явного «да» владельца по каждому репозиторию отдельно (Confirmation Gate, три вопроса — Avitolog/Legal Constructor/AI Intelligence, все «Да, делай»):
  - **AI Avitolog** — `avito-listing-packaging` разбит на `media-plan/SKILL.md` и `market-recon/SKILL.md`, тем же методом, что Модули 1/3/4. `module_headers: 0` подтверждён повторным прогоном.
  - **AI Legal Constructor** — хуки `guard-rules-bloat.sh`/`block-dangerous-git.sh` перенесены и подключены в `.claude/settings.json`; `direct-reading-policy.md`+`direct-response-policy.md`+`verify-before-analyze.md` слиты в `data-discipline.md` (3 файла, не 4 — `ask-before-searching.md` там не существовал, раздел «Недостающие данные» добавлен новым).
  - **AI Intelligence** — то же самое, но перенос осмысленным объединением, не слепой копией: этот дом держит канон в `instructions/`, не в `.claude/rules/` (старый Foundation-канон) — `guard-rules-bloat.sh` адаптирован (путь матчинга и потолок файлов пересчитаны под `instructions/`), проверено прогоном хука на фейковых путях (обычная запись проходит, инцидент-нарратив блокируется, путь вне `instructions/` не трогается).
- `knowledge/agent-registry.md` обновлён по факту (не по намерению) — все три таблицы (Composability, Confirmation Gate, Hooks+data-discipline) теперь без прочерков по сегодняшним изменениям канона. Propagation Sweep (`02-execution-gates.md`) пройден целиком, не только для агента, ради которого правка делалась.

### Решения
- Ни один из 12 флагов не требовал нового `tools/`-скрипта в самом скилле — это само по себе находка: гейт больше рисковал плодить ложные срочные задачи по багу собственного тула, чем реальные пробелы. Регулярка — источник правды, не список скиллов.
