# Дневник 2026-08-12

## Карта Foundation как связанной системы, пилот Exit Gate, ремедиация repository-design

### Что сделали

- Построили карту Foundation по цепочке задача → понимает → читает/использует → выполняет → формирует результат → сохраняет узнанное → принимает решение → обновляется → следующая задача, с FACT/CONNECTION/GAP/UNKNOWN по каждому переходу, не по файлам поодиночке. Подтверждены два реальных разрыва в одном узле («формирует результат»): нет проводки в `workspace/`, нет проверки доказательства перед «готово» (Exit Gate). Найдено, что узел «сохраняет → принимает решение» не линеен, как в исходной схеме — проверка значимости решения происходит на входе в задачу, а не как следствие записи в память.
- По найденным разрывам отклонили собственный первый вариант калибровки Exit Gate (reuse Impact-оси Entry Gate) как непроверенную архитектурную гипотезу, выданную за продолжение находки Researcher. Приняли как рабочую гипотезу Вариант 3 — без отдельной оси риска, триггер = граница `workspace/` (то же самое различие «черновик/результат», что уже нужно для проводки).
- Прогнали гипотезу на реальной задаче: применили `repository-design`'s чек-лист «репозиторий готов» к самому Engineer. Результат — `workspace/2026-08-12-repository-design-audit.md`. Нашли: (1) пункт 1 чек-листа противоречил собственной таблице Implementation (HOME.md «Кто ты» — точка расширения, а пункт требовал «не изменено») — исправлено, повторный прогон подтвердил отсутствие новых противоречий; (2) 3 Skills установлены не через `installer` — 2 из 3 (`decision-documentation`, `tool-selection`) досинхронизированы (добавлен `evidence.json`, `SKILL.md` подтверждён неизменным по sha256); третий (`repository-design`) оказался не отстающим, а опередившим канон — правки этой же сессии; (3) пункт 10 (сверка с эталоном Researcher) не может закрываться декларацией — остаётся открытым до реального обмена с Researcher.
- Передали Researcher вопрос о соответствии чек-листа `repository-design` реальной практике (критерий зафиксирован до поиска). Ответ: 3 пункта чек-листа подтверждены практикой с источниками; 3 категории отсутствуют системно — least-privilege/allowed-tools, воспроизводимый (перезапускаемый) eval, структурная независимость producer≠reviewer. Записано как наблюдения в новый `knowledge/repository-design-observations.md`, не как Proposal.
- Владелец принял решения без нового исследования: (а) три observations — не вносить в `repository-design` сейчас, Proposal не создавать, Researcher доказала наличие blind spots, но не то, что покрывать их должен именно этот Skill; (б) `repository-design` не синхронизировать с канонической версией Skill System сейчас — локальная версия остаётся рабочей, расхождение намеренно. Обе записаны как `Locked` в `DECISIONS.md`.
- **Важная правка от владельца, не поймана самостоятельно**: назвала Skill System «складом» — обесценивающая рамка. При проверке (`Skill System/SKILL-SYSTEM.md`) нашли, что это не хранилище файлов, а система с evidence-loop (Evaluation → Human approval → Verified → Usage → Review → Re-evaluation), честной семантикой статусов (`DRAFT` ≠ «плохой»), и — важнее всего — уже готовой моделью отношений Engineer↔Skill System (§11): «Инженер отвечает за синхронизацию локальных Skills агентов с каноническими версиями — по команде на обновление, не автоматически»; «локальная копия... не становится новым источником истины». Решение владельца про `repository-design` выше переписано со ссылкой на этот источник — оно оказалось не осторожной догадкой, а прямым применением уже спроектированной модели.

### Что осталось

- Пункт 6 чек-листа (`ROUTING.md` не покрывает главные сценарии специализации, только сценарии про себя) — остаётся отдельным, явно не закрытым вопросом. Владелец прямо просил не считать его закрытым по инерции вместе с остальной веткой.
- Три observations про `repository-design` — без носителя и без Proposal, ждут отдельного решения владельца, каким механизмом (если вообще) закрывать.
- Локальная и каноническая версии `repository-design` остаются разными — намеренно, не как открытый вопрос синхронизации, а как принятый и зафиксированный риск (см. `SKILL-SYSTEM.md` §11, «честное ограничение модели»).
- Ветка ремедиации Engineer закрыта владельцем по текущему объёму — не потому что Engineer идеален, а потому что цикл (аудит → дефекты → исправления → повторная проверка → Researcher validation → observations → решения → стоп) пройден целиком.

### Решения

- Вариант 3 калибровки Exit Gate — рабочая гипотеза для проверки, не `Locked`-решение об изменении Foundation. Foundation не менялась.
- `DECISIONS.md`, 2026-08-12: синхронизация `evidence.json` для 2 скиллов — `Locked`. Расхождение `repository-design` со складом — `Locked` (не синхронизировать сейчас, со ссылкой на `SKILL-SYSTEM.md` §11). Три observations Researcher — `Locked` (не вносить в Skill сейчас).
- `OPEN_QUESTIONS.md`: оба вопроса этого дня (Exit Gate — мировая практика; соответствие `repository-design` практике) закрыты с источниками.
