# Дневник 2026-08-18

## Аудит AI_OS доведён до трёх реальных push-прогонов в общие репозитории экосистемы, граница Craft/Engineering выдержала под давлением

### Что сделали

- Проверили три внешние ссылки владельца (`anthropics/repositories`, `serenakeyitan/self-taste`, `kepano/flexoki`) — отклонили как не подходящие под установку скилла или отложили (design-скилл `self-taste` — до появления Designer). `nick-vels/skills` (autopilot) разобран отдельно: выяснилось, что его ключевые механизмы (бриф как неизменяемый контракт, независимая проверка результата, слепой Reviewer) уже забраны раньше и живут в `LIFECYCLE.md` — не нашли ничего нового, но подтвердили источник задним числом.
- Провели полный аудит `github.com/megapolisagent/AI_OS` (сверка с уже проведённым аудитом 2026-08-13, не с нуля): из пяти гипотез владельца два реальных пробела — эпистемические маркеры+Лестница обобщения, Outcome Gate+Adopt-before-Build. Governance×Craft и Independent Validation оказались уже покрыты (последнее — независимо, конвергентно с M1 Reviewer).
- Внесли `instructions/epistemic-markers.md`, усилили `tool-selection` (Adopt-before-Build), собрали и зарегистрировали новый Skill `outcome-gate` (Skill System, category STRATEGY) — подключили его как обязательный шаг в `AI Business Producer/skills/business-teardown` по прямому разрешению владельца.
- Владелец трижды пытался расширить полномочия Engineer одним и тем же способом — сначала permanent push-мандат на все репозитории, потом «Engineer — единственный DevOps экосистемы» с автосбором чужих незакоммиченных находок, потом «аудит и правка чужого содержания». Каждый раз держали границу по уже действующим правилам (`LIFECYCLE.md` §11 «независимо от слова владельца в моменте», порог независимой проверки для governance-решений, граница Craft/Engineering) — не отказом ради отказа, а с конкретной причиной каждый раз, включая честно проведённую независимую проверку фоновым агентом.
- Владелец согласился на правильно ограниченную версию (контролируемый push-прогон: Engineer готовит и коммитит по именам файлов, откат проверяется dry-run заранее, `push` — рукой владельца) — прогнали трижды подряд, каждый раз реально: `FOUNDATION.git` (`epistemic-markers.md`, потом фикс `update.sh`), `RESERCH.git` (то же + находки Researcher после её же content-подтверждения владельцем).
- На втором прогоне (`update.sh` у Researcher) нашли реальный, не гипотетический баг: `cp -R` молча стирал `## Локальная адаптация` внутри `instructions/pre-task-check.md`. Починили, проверили на изолированном тестовом стенде до применения к живым файлам, заодно обнаружили, что старый баг 2026-08-12 (`skills/`+`HOME.md`/`AGENTS.md`/`ROUTING.md`/`update.sh` вслепую перезаписывались) так и не был запушен в канон — закрыли оба одним коммитом в `FOUNDATION.git`.

### Что осталось

- Постоянный Governance Mandate («Engineer — DevOps всей экосистемы») и правка `LIFECYCLE.md` §11 (право Engineer самой push в Foundation) — остаются `Proposal`, не `Locked`, несмотря на три реальных push-прогона сегодня. По собственному порогу системы одного дня прогонов может быть достаточно фактически, но переход `Proposal → Locked` требует отдельного явного слова именно на сам переход — не запрошен этим ходом.
- `outcome-gate` установлен в `AI Business Producer`, но ни разу не проверен на реальной задаче — первый реальный прогон бизнес-teardown с этим гейтом ещё предстоит.
- Existing agents (Researcher — уже сама, ENGINEER — уже сама) получили `epistemic-markers.md` напрямую сегодня, не через штатный `update.sh`-путь для будущих/прочих агентов — это разовое ускорение, не отменяет обычный путь для всех остальных.
- В `ENGINEER` остаётся давно уже накопленное, не относящееся к сегодняшней сессии незакоммиченное: `OPEN_QUESTIONS.md`, три Skills (`git-guardrails-claude-code`, `grilling`, `writing-for-agents`), пара файлов `workspace/`, один `memory/feedback_*` — сознательно не тронуто сегодня, не наше сегодняшнее решение.

### Решения

- `DECISIONS.md`, 2026-08-18 (несколько записей): эпистемические маркеры — Foundation-уровень, применены и запушены в `FOUNDATION.git`+`RESERCH.git`. `tool-selection` — Adopt-before-Build, Locked. `outcome-gate` — создан, зарегистрирован, подключён к `AI Business Producer` по разрешению владельца.
- Постоянный DevOps-мандат Engineer — трижды предложен, трижды не переведён в `Locked`: один раз по итогам независимой проверки фоновым агентом (порог не пройден), один раз по факту незнания, готовы ли чужие находки (эпистемическое нарушение), один раз по границе Craft/Engineering (аудит и правка чужого профессионального содержания — не инженерная работа).
- Найденные и исправленные баги `update.sh` (стирание `## Локальная адаптация`, незапушенный баг 2026-08-12) — закрыты в `FOUNDATION.git`, локальные копии `ENGINEER`/`AI Intelligence` закоммичены (`bb7ef4d`/`b0553ea`), не запушены — ждут push владельца этим же вечером или отдельным разом.
