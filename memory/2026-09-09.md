# Дневник 2026-09-09

## Первый день, когда память ENGINEER стала реальными файлами на диске, а не только правилом

### Что сделали
- Провели экспресс-аудит по протоколу Challenger vs Champion: `handoff`, `find-skills` (заблокирован на read-only `find`, `add`/`init`/`update`/`sync` категорически запрещены — `add` по умолчанию делает symlink), докупили `yagni-ladder` — собран заново через полный цикл `skill-authoring` (реальные RED/GREEN на живых субагентах, не имитация), статус сразу `VERIFIED`.
- Зафиксировали архитектурный закон: symlink при монтировании скиллов запрещён категорически, только физическая копия (CLAUDE.md §7).
- Переписали закон внешней разведки: обязательна всегда при сборке/модернизации агента, не только когда дома пусто — три источника + Platform Decision Record с явным вердиктом (`capability-resolver.md`).
- Провели инвентаризацию MCP: убрали Firecrawl (машиноширокий user scope), починили мёртвую ссылку на дубль `exa` в `enabledMcpjsonServers`, Gmail/Calendar/Drive остались — они account-level коннекторы claude.ai, локально не отключаются.
- Установили `gws` (Google Workspace CLI) глобально, проверили в обоих шеллах — но авторизацию заморозили: `gcloud` не тянем без реальной задачи (`yagni-ladder`, рунг 1).
- Аудировали `docling` (документы→markdown) и связку `mem0` / `llm-wiki` (память агентов) — ничего не поставили, только вердикты: `docling` чист, но диск C: забит на 95,7% — сначала это; `mem0` отклонили (шлёт реальные сообщения в облако, требует платный ключ); `llm-wiki` (SamurAIGPT/llm-wiki-agent) — хороший паттерн под наш `knowledge/`, но только agent-mode, без их `litellm`/`ANTHROPIC_API_KEY`-скриптов.
- Обнаружили и закрыли структурный пробел: `MEMORY.md`, `memory/`, `knowledge/` были только текстом в `memory-rules.md`, реальных каталогов не было. Завели сейчас.
- Владелец подтвердил: `HOME.md`/`ROUTING.md`/`SOUL.md`/`PROFILE.md` удалены осознанно — вычистили все ссылки на них из `.claude/rules/*.md`, заменили реальными адресами (в основном на `CLAUDE.md` §3).
- Синтезировали `knowledge-wiki` (механизм llm-wiki + таксономия mem0) через полный цикл `skill-authoring` с реальными RED/GREEN на субагентах, зарегистрировали в Skill System (`VERIFIED`), физически смонтировали, наполнили первым реальным содержанием (3 инженерные находки дня).
- Провели ещё три Challenger-аудита по прямому запросу владельца: `graphify` (Graphify-Labs) — не Champion-замена knowledge-wiki, другая ниша, не установлен (тихая самоустановка, skill.md 713 строк, автовписывание в CLAUDE.md); `magic-mcp`/21st MCP — платный облачный фронтенд-сервис без адресата в текущих агентах, не установлен; `yt-dlp` — зрелый и ответственный проект, но реальный RCE-паттерн через `--exec`/`--netrc-cmd`, конкретной задачи нет, не установлен. Все три вердикта записаны в `knowledge/sources/`.
- Провели полный самоаудит системы (правила, `knowledge/`-линт, дрейф смонтированных скиллов, конфиги, хуки) перед сборкой боевых агентов. Нашли и закрыли: дохлый хук `entry-gate-check.py` (охранял навсегда удалённый `HOME.md`) — удалён вместе с привязкой в `settings.json`; дохлую ссылку в `reviewer.md` — поправлена; всю цепочку `DECISIONS.md`/`LIFECYCLE.md`/`UPDATE.md`/`workspace/` — рудименты той же старой схемы, владелец подтвердил удаление, вычистили из 5 правил; 6 скиллов без следа в `registry.json` Skill System — `skill-authoring`/`skill-auditor` зафиксированы как инфраструктура движка (CLAUDE.md §7), остальные 4 уже были в `skills-lock.json` (расхождение оказалось только в моём первом отчёте, не в реальности); мёртвые пути к несуществующей папке `AI Intelligence/Foundation/` в `settings.local.json` — вычищены.
- Владелец перенесла репозиторий `FOUNDATION` на Рабочий стол, сама выбросила монолитные шаблоны. Разобрала лично оставшиеся `DECISIONS.md` + 7 файлов `instructions/`: все 7 оказались более старыми, не почищенными снимками того, что уже есть в `.claude/rules/` — переносить контент не пришлось, только сверить. Из `DECISIONS.md` (5 Locked + 1 Proposal) новым оказалось одно — принцип пропорциональной проверки по радиусу поражения, перенесён как Proportional Review Gate в `pre-task-check.md`. Заодно вычистила последнее упоминание `update.sh` (из `epistemic-markers.md`) и перепроверила `settings.json`/`skills-lock.json` FOUNDATION vs ENGINEER — расхождений нет. Разбор — `knowledge/sources/foundation-decisions-merge.md`.

### Что осталось
- ~~`HOME.md`, `ROUTING.md`, `SOUL.md`, `PROFILE.md` — не трогали~~ — закрыто в этот же день, см. выше.
- ~~Решение по `llm-wiki`-адаптации не принято~~ — закрыто, см. `knowledge-wiki` выше.
- Освобождение диска (4,3 ГБ свободно из 100 ГБ) — не наша задача сегодня, но названо как блокер для любых будущих локальных моделей/весов.
- `pre-task-check.md`/`capability-resolver.md`/`external-script-safety.md` всё ещё ссылаются на `instructions/<файл>.md` вместо реального `.claude/rules/<файл>.md` — теперь понятно, откуда взялось имя (буквально путь из репозитория FOUNDATION, скопирован вместе с текстом правил и не адаптирован) — правка на будущее, не входила в сегодняшний объём.
- `docling` — аудит дан, установка отложена по слову владельца («потом вернёмся»), не забыть.
- Папка `FOUNDATION` на Рабочем столе не удалена — решение об этом (удалить/архивировать) за владельцем.

### Решения
- Symlink при монтировании скиллов — запрещён категорически, только физическая копия (владелец, 2026-09-09).
- Внешняя разведка — обязательна всегда, не по остаточному принципу (владелец, 2026-09-09).
- `gws`: установлен, авторизация заморожена до реальной задачи под Sheets/Drive/Calendar; `gcloud` не ставим.
- `mem0` (плагин Claude Code) — не берём, архитектурно облачный сервис.
- Память ENGINEER — с сегодняшнего дня официально живёт в `MEMORY.md`/`memory/`/`knowledge/` этого репозитория, не только во встроенной авто-памяти среды.
- `DECISIONS.md`/`LIFECYCLE.md`/`UPDATE.md`/`workspace/` — рудименты старой Foundation-схемы, удалены осознанно, не восстанавливать; источники истины теперь только `MEMORY.md` и `knowledge/` (владелец, 2026-09-09).
- `skill-authoring`/`skill-auditor` — инфраструктура движка, не каталог Skill System; остальные внешние утилиты фиксируются в `skills-lock.json` (владелец, 2026-09-09).
- FOUNDATION-репозиторий на Рабочем столе не удаляется этой сессией — только читается; полезный остаток (Proportional Review Gate) уже перенесён в `.claude/rules/`.
