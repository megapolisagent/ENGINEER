# Дневник 2026-08-31

## Юридический отдел дашборда заработал, собран новый агент «Авитолог»

### Что сделали
- Починили путаницу: реальный дашборд — `МЕГАПОЛИС ДАШБОРД` (кириллица), не `MEGAPOLIS_DASHBOARD` — юрист изначально писал не туда.
- Нашли: дашборд не читает файлы с диска, контент собирается вручную в `docs_content.js` — написали `scripts/publish_docs.py`, который это автоматизирует. Добавили в UI дашборда drill-down «папка → документы» (владелец явно попросила, не было изначально).
- Дописали юристу (`AI Legal Constructor/HOME.md`) короткую инструкцию — его `docPath` и команда публикации, чтобы дальше справлялся сам, без Engineer.
- Собран новый агент `AI Avitolog` — упаковка карточек/медиаплан/калькулятор офферов/чат-воронка под Avito. Три раза подряд владелец в разных формулировках просила подключить автоматический сбор данных с Avito (API/Playwright/OpenClaw) — каждый раз отказано, зафиксировано `Locked` в `DECISIONS.md` агента.

### Что осталось
- Полный чек-лист «агент готов к реальной работе» для Avitolog не пройден — нужен реальный прогон на данных владелицы (условия объекта или файл выдачи Avito), запрос эталона у Researcher.
- Ресерчер отправлен на задачу про Авитолога раньше в разговоре — задача про Researcher (владелец просила «пусть речерч ищет») выполнена для легальных каналов данных, не переоткрывать.
- Старая копия файлов юриста в неверном месте — удалена по слову владельца.

### Решения
- `rm -rf` после `diff -rq` под `set -e` не гарантированно останавливается на расхождении — записано в `knowledge/lessons-learned.md`, проверять явно перед разрушительной командой.
- Автоматический сбор данных с Avito — не строится ни при какой формулировке задачи, юридический риск подтверждён дважды (Engineer + Researcher).
