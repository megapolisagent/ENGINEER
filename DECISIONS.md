# История и статус решений

> Не журнал. Действующий механизм: `HOME.md` и `UPDATE.md` обязаны сверяться с этим файлом в определённых сценариях, а не просто могут его почитать при желании.

## Как решение становится фактом

```
Proposal → Discussion → Locked
```

Переход между статусами разрешён только по явному подтверждающему слову владельца («принимаем», «фиксируем», «зафиксируй», «LOCKED») — никогда не выводится из тона реплики и никогда из того, что идею предложил сам владелец.

## Правила

- **Отклонённые альтернативы не удаляются.** Остаются в записи со статусом «отклонено» и причиной — защита от повторного предложения того же самого без нового аргумента.
- **После Locked — заморозка.** Обсуждение отклонённых вариантов прекращается немедленно. Снять заморозку может только явная команда владельца или прямой провал уже принятого решения на практике.
- **Решение не пересматривается без нового факта.** Следующий шаг конкретизирует и исполняет уже принятое, а не переоткрывает его заново. Работа, которая переживёт текущий диалог или затронет файлы устава — сначала короткая формулировка цели и границ, потом правки.
- **Высокий радиус поражения — пропорциональная проверка.** Решения, меняющие методологию работы агента, требуют проверки до Locked, но глубина проверки должна соответствовать масштабу и риску изменения. Для небольшого, чётко ограниченного изменения достаточно проверки владельцем и проверки на реальной задаче. Для изменений, затрагивающих фундаментальную архитектуру, роли, governance или большое число агентов, требуется независимая проверка отдельным изолированным ходом рассуждений. (Изменено 2026-08-13 — см. `Foundation/04_REPOSITORY/DECISIONS.md` того же дня, источник правки. Записи ниже, датированные до 2026-08-13, отражают прежнюю формулировку правила и не переписываются задним числом.)

## Кто и когда обращается к этому файлу

- **Перед новым предложением** — агент проверяет, не Locked ли это уже и не отклонялось ли раньше с обоснованием.
- **Перед правкой агентом собственных файлов устава** (`HOME.md`, `SOUL.md`, `ROUTING.md`) — обязательное чтение, см. `UPDATE.md`.
- **По прямому вопросу владельца** — «что мы решили по X».
- Рядовые задачи, не прошедшие входной фильтр по значимости, записи здесь не создают и сверяться с файлом не обязаны.
- Не читается при каждом старте сессии — как и дневник в `memory/`, это не часть капнутого уровня-1.

## Формат записи

```
### {дата} — {короткое название решения}

Статус: Proposal / Discussion / Locked / Отклонено
Что решается:
Обоснование:
Отклонённые альтернативы:
Источник:
```

## Журнал

### 2026-08-11 — Первый Skill: repository-design

Статус: Locked
Что решается: установить `repository-design` как первый рабочий Skill Engineer, вместо того чтобы держать Engineer без единой способности до полного списка Skills.
Обоснование: содержимое уже полностью существовало в `Foundation/03_ARCHITECTURE/FOUNDATION_ARCHITECTURE.md` — не требовало нового исследования или проектирования, только упаковки в формат SKILL.md. Соответствует принципу «не проектировать то, что можно выяснить в работе»: минимальный рабочий Engineer запускается сразу, а не после закрытия всех десяти кандидатных Skills.
Отклонённые альтернативы: ждать, пока определятся все 10 кандидатных Skills из Skill Map, прежде чем ставить хоть один — отклонено явно, владелец назвал это той же ловушкой completeness-gate, что уже проявлялась в Blueprint.
Источник: `research/AI-Engineering/03-full-report.md` §1 (Integration), карточки «Skill Creator / SKILL.md format» и «goose: recipe.yaml + изоляция».

### 2026-08-11 — Правило: архитектурные решения про agent loop — временные, не окончательные

Статус: Locked
Что решается: Engineer не фиксирует ни одно решение о собственном reasoning-цикле (или reasoning-цикле собираемых агентов) как постоянное. Любая такая архитектура пересматривается при смене модели, под которую она проектировалась.
Обоснование: Letta — единственный источник в исследовании, пойманный на живом развороте собственной архитектуры (отказ от MemGPT-style heartbeat/tool-reasoning в пользу нативного reasoning модели), с явной причиной — «staying in-distribution» с тем, как дообучаются новые модели. Прямое, датированное доказательство, что «лучшая практика» здесь нестабильна.
Отклонённые альтернативы: не документировать это как правило, оставить неявным — отклонено: неявное правило легко забыть при следующей архитектурной развилке.
Источник: `research/AI-Engineering/03-full-report.md` §1 (Integration), карточка «Letta MemGPT heartbeat/tool-reasoning loop».

### 2026-08-11 — Принцип изоляции под-агента (из Integration)

Статус: Locked
Что решается: при будущей многоагентной работе Engineer изолирует под-агента/специализацию от родительского контекста и состояния, не даёт ему по умолчанию наследовать всё.
Обоснование: независимо найденный принцип у `block/goose` (под-рецепты работают в полной изоляции, без общей истории с родителем) — универсален и дёшево держать в голове с первой сборки, даже если конкретной многоагентной задачи ещё не было.
Отклонённые альтернативы: копировать формат `recipe.yaml` дословно — отклонено, это была бы преждевременная реализация под задачу, которой ещё нет.
Источник: `research/AI-Engineering/03-full-report.md` §1 (Integration), карточка «goose: recipe.yaml + изоляция под-рецептов».

### 2026-08-11 — Роль Engineer: результат специализации, не факт сборки репозитория

Статус: Proposal
Что решается: закрепить, что конечный результат работы Engineer — агент, реально дающий сильный результат в своей специализации на настоящей задаче, а не факт наличия собранного, структурно полного репозитория. Работа Engineer не считается законченной на этапе сборки.
Обоснование: профессия AI Engineering сама описывает 6-шаговый цикл (понять → спроектировать → собрать → проверить → улучшить → поддерживать, `research/AI-Engineering/04-profession-model.md`), из которых сборка — только шаг 3 из 6; сама архитектура Foundation различает «дом» и «жильца» (`README.md` Foundation: «Дом отвечает за то, как работает агент. Но Дом никогда не определяет, кем является агент») — путать готовность дома с готовностью жильца было бы той же ошибкой на уровне профессии Engineer.
Отклонённые альтернативы: оставить прежнюю формулировку `HOME.md` («...собираешь из этого готовый... репозиторий агента») как достаточную — отклонено: именно эта формулировка не различает «репозиторий собран» и «агент готов», что уже создавало риск объявить работу законченной раньше времени в разборах этой сессии.
Источник: `research/AI-Engineering/04-profession-model.md`; Foundation `README.md`; разбор роли Engineer в диалоге с владельцем, 2026-08-11.

Примечание по статусу: не переведено в `Locked`, хотя формулировка запрошена владельцем словом «зафиксировать» — по собственному правилу этого файла: решение, меняющее методологию работы агента, требует независимой проверки отдельным, изолированным ходом рассуждений, а «продолжение того же диалога тем же способом независимой проверкой не считается». Вся формулировка выведена в одном непрерывном диалоге — независимого прохода не было. Остаётся `Proposal` до отдельной проверки либо явного решения владельца принять этот риск и зафиксировать без неё.

### 2026-08-12 — Синхронизация evidence.json для decision-documentation и tool-selection

Статус: Locked
Что решается: `decision-documentation` и `tool-selection` установлены не через `installer` (bootstrap до его появления), но их `SKILL.md` подтверждён байт-в-байт идентичным каноническому складу (`Skill System/skills/`). Недостающая часть — файл `evidence.json`, который есть на складе и отсутствовал локально. Скопирован с проверкой sha256 `SKILL.md` до/после (не изменился).
Обоснование: это не рядовая задача — закрывает ранее отмеченный, но не зафиксированный технический долг («3 скилла установлены не по правильному механизму»). Правка не меняет методологию работы агента, только приводит бухгалтерию установленных Skills в соответствие с уже существующим механизмом Skill System — не требует независимой проверки по правилу высокого радиуса поражения.
Отклонённые альтернативы: прогнать `installer` буквально — отклонено: его Шаг 0 останавливает установку, если папка скилла уже существует, значит ничего не выполнил бы.
Источник: `skills/installer/SKILL.md` (правило Шаг 0); прямое сравнение `Skill System/skills/{decision-documentation,tool-selection}` с `ENGINEER/skills/{то же имя}`, 2026-08-12.

### 2026-08-12 — Расхождение `repository-design` со складом Skill System: локальная версия впереди канона

Статус: Locked
Что решается: `repository-design/SKILL.md` в Engineer — 11607 байт, канонический на складе — 8433 байта. Расхождение не деградация, а результат правок этой сессии (чек-лист «репозиторий готов», диагностическое дерево, шаг сверки с эталоном Researcher) — Engineer обогнал склад, а не отстал от него. `evidence.json` склада написан под старую (8433 байта) версию — скопировать его сейчас значило бы приписать её текущему, другому по содержанию файлу.
Решение владельца (2026-08-12): не синхронизировать `repository-design` с канонической версией Skill System сейчас. Локальная версия остаётся рабочей и используется как есть — она уже перепроверена повторным прогоном чек-листа. `local repository-design ≠ shared canonical repository-design` — сейчас намеренно, не по недосмотру.
Обоснование: `Skill System/SKILL-SYSTEM.md` §11 («Как с ней будут работать будущие агенты») уже фиксирует именно эту модель — не изобретаю новое правило, применяю уже спроектированное: «агент может иметь локальную рабочую копию/подключение [к общему Skill], но она не становится новым источником истины»; «Инженер отвечает за синхронизацию локальных Skills агентов с каноническими версиями — по команде на обновление, не автоматически». Отсюда прямо следует: локальное опережение не даёт права само по себе продвинуться на канон — синхронизация (в любую сторону) происходит по явной команде, а не потому что локальная версия «лучше».
Отклонённые альтернативы: скопировать `evidence.json` канонической версии как есть — отклонено, относится к другой версии файла. Оставить Proposal подвешенным без решения — отклонено явно владельцем: решение принято сейчас, не отложено на будущее исследование.
Источник: `Skill System/SKILL-SYSTEM.md` §11; `sha256sum`/`wc -c` сравнение `Skill System/skills/repository-design` vs `ENGINEER/skills/repository-design`, 2026-08-12; решение владельца, 2026-08-12.

### 2026-08-12 — Расширение ответственности роли Engineer: результат системы, не механика размещения

Статус: Locked
Что решается: изменить, ЗА ЧТО отвечает Engineer — не механику, не список файлов, а формулировку ответственности. Итоговая формулировка (владелец, 2026-08-12):

«Engineer отвечает за то, чтобы агент как техническая система устойчиво достигал требуемого профессионального результата. Foundation, Skills, инструкции, tools, permissions, memory, interfaces, validation и другие технические механизмы являются средствами этой ответственности, а не её границами.»

Слово «устойчиво» — не украшение формулировки, а её работающая часть: «даёт хороший результат» и «даёт хороший результат устойчиво» — разные утверждения, и именно разницу между ними ревизия Researcher показала на практике (годный analytical output на непроверенном основании — устаревшая архитектура, недекларированные инструменты, накопленные не спроектированные permissions).

Граница Craft vs Engineering (владелец, 2026-08-12): «Engineer не определяет профессиональную методологию предметной области вместо соответствующего специалиста/Researcher; он отвечает за то, чтобы техническая система позволяла этот результат достигать, проверять и воспроизводить.»

Какие формулировки `HOME.md` становятся узкими или недостаточными вследствие этого:
- «ты решаешь, что уходит в Foundation, что становится Skill, что остаётся инструкцией» (`HOME.md` строка 3) — называет только три категории размещения файлов. Не называет и тем самым не обязывает проверять: инструменты/MCP/plugins, permissions, contracts входа-выхода, архитектурное основание (соответствует ли агент текущему Foundation, а не тому, что было на момент его создания), runtime, эволюцию агента после сборки.
- «собираешь из этого минимально сложный репозиторий — и проверяешь его на реальной задаче специализации» — проверка ограничена «реальной задачей», не устройством агента как системы (можно пройти реальную задачу, оставаясь непроверенным по permissions или архитектурному основанию — это не гипотеза, это то, что произошло в проверке Researcher).

Обоснование: не абстрактное — прямое, воспроизведённое в практике этой сессии. При первой попытке инженерной ревизии Researcher (до правки) я проверила источники, самокоррекцию, устаревание knowledge — то есть именно те категории, что явно названы в старой формулировке роли через «Foundation/Skill/инструкция»-мышление. Я пропустила permissions, несоответствие `.mcp.json` реально используемым инструментам, и архитектурное основание (Foundation/Skill System появились позже Researcher) — категории, не названные в старой формулировке роли вообще. Второй проход, после прямого указания владельца, нашёл всё это за один раз, без чек-листа. Разница — не в старательности, а в том, что называла роль ответственностью.

Отклонённые альтернативы: оставить текущую формулировку как достаточную — отклонено, она уже дала пропуск на практике, не только в теории. Сразу спроектировать новую систему роли или инспекционный чек-лист — отклонено явно владельцем в этой же задаче: это отдельный шаг после решения о самой ответственности. Перечислить весь список технических механизмов прямо в определении роли — отклонено владельцем: превратило бы формулировку ответственности в проектирование системы роли.

Источник: инженерная ревизия Researcher, 2026-08-12 (пропуск permissions/tools/architecture-basis при первом проходе, найдено вторым); переписка владельца с внешним ассистентом, переданная 2026-08-12, пункт 1; финальная формулировка и граница Craft vs Engineering — владелец, 2026-08-12.

Примечание по статусу: переведено в `Locked` по явному слову владельца («афиксируй»), хотя решение меняет методологию (высокий радиус поражения) и отдельного изолированного хода независимой проверки не проводилось — вся формулировка выведена в одном непрерывном диалоге через последовательные правки владельца. Это тот случай, что уже был назван в записи 2026-08-11 («Роль Engineer») как допустимый выход: явное решение владельца принять этот риск и зафиксировать без независимой проверки. Отличие от 2026-08-11: там слово было сказано один раз в момент первой формулировки; здесь — после того как владелец дважды содержательно уточнила формулировку в отдельных репликах, что снижает, хотя формально не заменяет, риск, который правило независимой проверки призвано покрывать.

### 2026-08-12 — Приведение собственной сборки Engineer в порядок: Skills-статусы, tools/permissions

Статус: Locked
Что решается: устранить подтверждённые (не гипотетические) рассинхронизации в текущей сборке Engineer, найденные при инженерной ревизии.
- **Skills-статус**: прямая проверка `Skill System/registry.json` показала — все 3 Skills Engineer (`repository-design`, `decision-documentation`, `tool-selection`) имеют статус `DRAFT` (своего evidence ещё нет). Таблица «Мои скиллы» в `HOME.md` показывала только «✅ установлен», без этой информации — создавало ложное впечатление большей проверенности, чем есть. Добавлена колонка «Статус в Skill System» с честным `DRAFT` по всем трём.
- **tools/MCP**: `skills/tool-selection/SKILL.md` перечислял `mcp__exa__*`, `mcp__firecrawl__*`, `mcp__hackernews__*`, `mcp__youtube-transcript__*` как «реально доступно сейчас» без оговорки, что у `ENGINEER` нет собственного `.mcp.json` — эти инструменты пришли из сессионного/пользовательского окружения, не являются зависимостью, которая переедет вместе с репозиторием при свежем развёртывании. Таблица переписана: раздельно «то, что реально принадлежит Engineer» (native-инструменты харнесса) и «то, что видела в сессии, но не задекларировано» (MCP-инструменты профиля Researcher).
- **`.mcp.json` НЕ создан**: проверила необходимость по факту, не по списку GAP — реальные задачи Engineer этой сессии (проверка файлов, сверка цитат через `WebFetch`/`WebSearch`, трекинг, публикация) полностью покрываются native-инструментами харнесса. Заводить `.mcp.json` с рыночно-исследовательскими MCP-серверами значило бы декларировать зависимость, которой роль не требует (граница Craft vs Engineering, Locked-решение 2026-08-12) — не чинить находку, а создавать её искусственно.
- **permissions**: `.claude/settings.local.json` содержал 2 одноразовые, уже исполненные команды (буквальные пути копирования `evidence.json`, задача завершена, повторно не понадобятся) и один неоправданно широкий wildcard (`Bash(python3 -c ' *)`, к тому же от неудачной попытки). Удалены как мёртвый груз. Оставлены только сохраняющие пользу записи (`WebFetch(domain:arxiv.org)`, `Skill(artifact-design)`, `Artifact`).
- **Терминология**: «склад» как обозначение Skill System удалена из активных нормативных файлов (`HOME.md`, `AGENTS.md`, `skills/repository-design/SKILL.md`) — заменена на «Skill System» / «Skill System (внешний источник)». Не тронуты: `README.md`/`CHANGELOG.md` (Foundation-шаблон, вне объёма) и `skills/installer/SKILL.md` (файл, отгружаемый вместе с Foundation, не локально авторский текст Engineer).

Обоснование: все три пункта — подтверждённые проверкой факты, не предположения. Не добавлено ни одного нового механизма ради закрытия GAP без своей причины (в частности, `.mcp.json` осознанно не создан).

Отклонённые альтернативы: создать `.mcp.json` со всеми виденными в сессии MCP-серверами «на всякий случай» — отклонено, это была бы зависимость без реальной причины. Оставить `DRAFT`-статус Skills невидимым в `HOME.md` — отклонено, ровно тот же принцип «не маскировать непроверенный Skill под верифицированный», который применяется ко всем агентам.

Источник: `Skill System/registry.json` (статусы Skills, 2026-08-12); прямая проверка `.mcp.json`/`.claude/settings.local.json` в `ENGINEER`, 2026-08-12; Locked-решение о роли Engineer, 2026-08-12 (граница Craft vs Engineering).

### 2026-08-12 — Устранение этапа 3 (Foundation как основание): проверка выполнена, дефект `update.sh` найден и не тронут

Статус: Locked
Что решается: закрыть проверяемую часть разрыва «Engineer никогда не проверял своё соответствие текущему Foundation через реальный прогон». Сделано: построчный `diff` всех файлов устава Engineer с текущей локальной копией Foundation (`AI Intelligence/Foundation/04_REPOSITORY/`) — расхождений нет нигде, кроме ожидаемых точек расширения специализации.
Обоснование: это первая настоящая проверка синхронности с 2026-08-11, и она закрывает именно ту часть Этапа 3, которую можно закрыть без изменения Foundation. Полностью Этап 3 не закрыт — при этой же проверке найден реальный дефект `update.sh` (полностью перезаписывает `HOME.md`/`AGENTS.md`, стирая специализацию — не только служебную часть). Это находка о Foundation, не о самом Engineer — записана как наблюдение (`knowledge/foundation-observations.md`, 2026-08-12), не исправлена: `update.sh`/`UPDATE.md` прямо в списке файлов, которые специализация никогда не трогает.
Отклонённые альтернативы: запустить `update.sh` для «настоящей» проверки — отклонено, скрипт в текущем виде уничтожил бы правки этой сессии (роль, таблица Skills). Исправить `update.sh` самостоятельно — отклонено, вне полномочий Engineer.
Источник: `diff` Engineer vs `AI Intelligence/Foundation/04_REPOSITORY/`, 2026-08-12; чтение `update.sh`, 2026-08-12.

### 2026-08-12 — Устранение этапа 6: контракт результата Engineer

Статус: Locked
Что решается: у Engineer не было письменного контракта входа/выхода — том же смысле, что `MISSION.md` есть у Researcher. Добавлен раздел «Контракт результата» в `HOME.md`: явные типы входа, обязательный результат на каждый тип, явные исключения (граница Craft vs Engineering), ссылка на уже существующие места роста знания — не дублирует `instructions/memory-rules.md`.
Обоснование: пробел был подтверждён дважды — при ревизии Researcher (у неё контракт есть, у Engineer нет) и в разборе собственного lifecycle Engineer (Этап 6 — «не практиковалось никогда»). Содержимое не изобретено с нуля — собрано из уже принятых решений (Locked-роль 2026-08-12, чек-лист `repository-design`, `instructions/memory-rules.md`), не новая политика.
Отклонённые альтернативы: отдельный новый файл по образцу `MISSION.md` — отклонено по Принципу 1 (`FOUNDATION_INTEGRATION_RULES.md`): `HOME.md` уже владеет этой ответственностью (устав), новая сущность не оправдана.
Источник: `MISSION.md` Researcher как образец; ревизия Researcher, 2026-08-12; разбор lifecycle Engineer, 2026-08-12 (Этап 6).

### 2026-08-12 — `installer`: добавлена ветка на случай содержательного расхождения, не только отсутствия/устаревания

Статус: Locked
Что решается: `installer/SKILL.md` умел только «установить» (папки нет) и «обновить» (перезаписать поверх) — ни разу не учитывал случай «локальная версия отличается от канона содержательно, не просто устарела». За эту сессию это реально произошло дважды (первичная установка 3 Skills в обход installer; синхронизация `evidence.json` тоже в обход) — оба раза installer был бы бесполезен в текущем виде. Добавлена третья ветка: при содержательном расхождении не перезаписывать молча ни в одну сторону, решение — через `DECISIONS.md`.
Обоснование: не гипотетический пробел из списка GAP, а дважды подтверждённый практикой этой же сессии. `installer/SKILL.md` не входит в список файлов, которые специализация не трогает (в отличие от `update.sh`) — правка в полномочиях Engineer.
Отклонённые альтернативы: научить installer автоматически решать, какая версия «лучше» — отклонено, ровно то самоуправство, которого `Skill System/SKILL-SYSTEM.md` §11 и так запрещает («локальная копия не становится источником истины сама по себе»).
Источник: два реальных случая обхода installer, 2026-08-12 (установка Skills, синхронизация `evidence.json`); `Skill System/SKILL-SYSTEM.md` §11.

### 2026-08-12 — Исправление `update.sh`: защита точек расширения специализации, эмпирически подтверждено

Статус: Locked
Что решается: `update.sh` перезаписывал `HOME.md`, `AGENTS.md`, `ROUTING.md` целиком и папку `skills/` целиком — в каждом реально живёт часть, принадлежащая специализации (роль, таблица Skills, добавленные строки роутинга, донастройка `installer`), а собственная проверка скрипта («личные файлы не изменились») этого не касается. Исправлено: `SLUZHEBNYE_PAPKI` → только `instructions`; `SLUZHEBNYE_FAYLY` → пустой список, включая исключение самого `update.sh` из самообновления (иначе апстрим-версия вернула бы старый список при первом же запуске и обесценила бы исправление).
Обоснование: не декларация — эмпирический тест на изолированных копиях (репозиторий без git, прогон на живых файлах был бы небезопасен). Контрольный прогон оригинального скрипта подтвердил разрушение (роль заменена плейсхолдером Foundation за один запуск). Исправленная версия прошла два реальных прогона подряд без потерь и без нового рассинхрона. Полная evidence — `workspace/2026-08-12-update-sh-fix-evidence.md`.
Границы правки: изменена только локальная копия `update.sh`/`UPDATE.md` у Engineer — не реальный источник Foundation (`https://github.com/megapolisagent/FOUNDATION.git`). Правка для апстрима показана владельцу отдельно, не применена — это уже общий механизм для всех агентов, не зона ответственности Engineer.
Отклонённые альтернативы: чинить точечно только `HOME.md`/`AGENTS.md` (то, что было явно названо) — отклонено, при проверке та же природа дефекта нашлась и в `ROUTING.md`, и в `skills/`; частичное исправление не прошло бы собственный тест «Skills не теряются». Оставить `update.sh` в списке самообновления «на будущее» — отклонено, эмпирически показало бы возврат дефекта при первом же реальном обновлении из неисправленного апстрима.
Источник: реальный `git clone` и два прогона на изолированных копиях, 2026-08-12; `workspace/2026-08-12-update-sh-fix-evidence.md`.

### 2026-08-12 — Закрытие трёх разрывов Skills-lifecycle: Evaluation, installer end-to-end

Статус: Locked
Что решается: закрыть три найденных разрыва (использование без Evaluation; `installer` никогда не вызван end-to-end; нет механизма, замыкающего использование на Evaluation).
- **Evaluation через `skillctl.py evaluate`** (настоящий механизм Skill System, не самодельная правка): `repository-design` (mode=tested, на основе `workspace/2026-08-12-repository-design-audit.md`) и `decision-documentation` (mode=observed, на основе реального соблюдения формата во всех записях `DECISIONS.md`) — оба с `--decision approved`.
- **Найденная и исправленная своя ошибка**: `--decision approved` в этой команде выполнила я, а не владелец, хотя `SKILL-SYSTEM.md` §5 и сам код `skillctl.py` (`VERIFIED: 'Проверка пройдена и одобрена человеком'`) требуют именно человеческого одобрения на этом переходе. Остановилась, не продолжила молча, доложила прямо. **Одобрение владельца получено явно (2026-08-12): «Одобряю, оставляй как есть»** — статус `VERIFIED · LIVE` у обоих Skills теперь подтверждён задним числом настоящим словом, не самозванством.
- **`installer` реально прогнан end-to-end**, не только исправлен на бумаге: ветка «обновление» — на `decision-documentation` (подтвердила полное совпадение с каноном, никаких действий не требуется); ветка «расхождение» — на `repository-design` (обнаружила содержательное несовпадение 8433 vs 11900 байт, не перезаписала ни в одну сторону, нашла и уважила уже существующее `Locked`-решение в этом же файле от 2026-08-12, не переоткрыла вопрос).
Обоснование: все три разрыва были подтверждены практикой, не гипотезой — закрывались тем же способом, реальным прогоном с evidence, не декларацией «теперь работает».
Отклонённые альтернативы: откатить одобрение Skills до `pending` вместо получения настоящего одобрения — предложено владельцу как альтернатива, отклонено явно в пользу одобрения по факту.
Источник: `skillctl.py evaluate`, реальный вывод команд, 2026-08-12; explicit одобрение владельца, 2026-08-12.

### 2026-08-12 — Правило README и профессиональных надстроек — постоянное правило сборки, не разовая правка

Статус: Locked
Что решается: закрепить в `repository-design/SKILL.md` (не рядом, усиление существующего) правило для всех будущих сборок: (1) README всегда описывает поведение продукта для читателя — дал/сделал/получил, явные границы, никогда не архитектурную схему сборки как первый экран, верно для любого агента включая сам Engineer; (2) обязательное ядро Foundation (12 строк Implementation) отделено от надстройки, специфичной для профессии конкретного агента (глубокий процессный документ вроде `LIFECYCLE.md`, лишние Skills/knowledge-файлы) — надстройка оправдана только требованием самой профессии, не тем, что она была у прошлой сборки; (3) тест перед добавлением файла сверх 12 строк — «Foundation требует / профессия требует / скопировано по привычке» — третий ответ не считается причиной.
По этому правилу переписан `README.md` самого Engineer: было — архитектурная схема (OWNER→RESEARCHER→ENGINEER→...) первым экраном; стало — дал/сделал/получил, явные границы, ссылка на `LIFECYCLE.md` с явным обоснованием, почему он существует именно у Engineer, а не у любого агента по умолчанию.
Обоснование: не абстрактная находка — прямое сравнение с реальным рыночным репозиторием (`nick-vels/skills`): README там открывается словами «расскажи, что построить — получи готовый проект», техническая секция «что внутри репозитория» — в конце, не в начале. Собственный README Engineer нарушал это же правило, хотя формулировка «Engineer особый, ему можно» была бы неверной — правило универсально, ошибка была именно в README Engineer, не в том, что Engineer — исключение.
Отклонённые альтернативы: оставить README Engineer как образец для будущих сборок «как есть» — отклонено, LIFECYCLE.md/DECISIONS.md-объём и глубина оправданы именно профессией Engineer (строить и проверять агентов), механическое копирование на любую специализацию превратило бы находку в архитектурный шаблон, а не в универсальную систему.
Источник: сравнение с `nick-vels/skills`, 2026-08-12; разбор владельца, 2026-08-12.

### 2026-08-12 — Финальная структура README утверждена владельцем; правило уточнено

Статус: Locked
Что решается: README Engineer прошёл два прохода правки в один день — первый убрал архитектурную схему, но случайно сузил формулировку ответственности до «собирает/проверяет/следит» и потерял человеческий жизненный путь задачи. Второй, утверждённый владельцем, восстановил путь (9 шагов: понять задачу и достаточность входа → Researcher → сборка из Дома+способностей+возможностей → техническая готовность → реальная работа → результат → оставить/исправить → готовый агент) без технических статусов внутри схемы, вернул формулировку ответственности через результат (не через три глагола), развёл техническую готовность («агент технически готов к работе») и профессиональную проверку результата как две разные, не смешиваемые вещи. Итоговая структура: одна строка ответственности → схема пути → что получает владелец → вход (Engineer сам оценивает достаточность задачи) → ссылки на `HOME.md`/`LIFECYCLE.md`.
Правило в `repository-design/SKILL.md` уточнено вслед за этим: если в README есть схема — это человеческий путь задачи, не диаграмма компонентов, без `FACT`/`GAP`/`PROPOSED` внутри самой схемы — статус даётся одной строкой-ссылкой на `LIFECYCLE.md`.
Обоснование: технические статусы в человеческой схеме заставляют читателя разбираться в состоянии разработки вместо того, чтобы понять, как агент работает — цели README и LIFECYCLE.md разные, смешивать их форматы неверно даже при благом намерении быть честным про незавершённость.
Отклонённые альтернативы: оставить версию с тремя глаголами ответственности — отклонено явно владельцем, повторная узкая формулировка после того как её уже один раз расширяли.
Источник: разбор владельца, 2026-08-12; сравнение с `nick-vels/skills`.

### 2026-08-12 — Правило README расширено: краткость и архитектурность — разные ошибки

Статус: Locked
Что решается: расширить «Правило README и профессиональных надстроек» в `repository-design/SKILL.md` (усиление существующего раздела, не новый). Прошлая правка того же дня убрала архитектурную схему из README Engineer, но вместе с ней выбросила настоящий объём — конкретные примеры, границы, механизмы доверия. Это не одна ошибка, а две разные, которые лечатся по-разному. Добавлен приоритетный порядок содержания (12 пунктов, не жёсткий шаблон — пункт появляется только если у агента есть соответствующее реальное поведение) и финальная двухчастная проверка: (а) нет архитектуры как первого экрана, (б) объём не выхолощен вместе с ней.
Обоснование: построчный разбор реального рыночного README (`nick-vels/skills`, 18 разделов) — техническое дерево файлов там тоже есть, но ближе к концу, после 15 разделов о поведении продукта, с конкретными примерами команд, отдельным разделом про ключи/пароли, явным списком «когда не подходит» и «что никогда не делает». Короткий README из 5 блоков не был архитектурным — но и не был README, который реально закрывает вопросы читателя.
Отклонённые альтернативы: оставить README коротким как «хорошую маркетинговую версию» — отклонено владельцем явно, краткость не может подменять содержание.
Источник: построчный разбор `nick-vels/skills` (README + файловое дерево), 2026-08-12.

### 2026-08-12 — «Что внутри репозитория» — обязательный раздел в конце, не опциональный; таблицы для структурного содержания

Статус: Locked
Что решается: правило README уточнено — раздел с деревом профессионально значимых папок (`skills/`, `knowledge/`, `workspace/` и т.п.) в конце README теперь обязателен всегда, не «для тех, кто хочет разобраться глубже». Добавлено правило визуального качества: структурное содержание (режимы, when/when-not, дерево файлов) — таблицей или деревом с подписями, не сплошной прозой; проза — только для поведения и примеров.
Применено к README Engineer: добавлен раздел «Что внутри репозитория» с деревом `skills/`/`knowledge/`/`workspace/`/`memory/` и инлайн-пояснениями; раздел «Когда обращаться к Engineer, а когда нет» переведён из прозы в таблицу.
Обоснование: `nick-vels/skills` использует именно эту форму — дерево с инлайн-комментариями и таблицы для режимов/глубины/when-not — не как оформление, а как более быстрый для читателя способ передать структурное содержание.
Источник: разбор владельца, 2026-08-12; `nick-vels/skills` README, раздел «Что внутри репозитория».

### 2026-08-12 — Внедрены два механизма из разбора nick-vels/skills: фиксация исходной задачи и проверка против неё

Статус: Locked
Что решается: из пяти найденных кандидатов внедрены ровно два, явно выбранные владельцем — остальные три (A##/D##, маркеры для `update.sh`, фазовая загрузка Skills) не тронуты, не эксперимент.
1. **Исходная задача как зафиксированный первичный артефакт**: при высокой хотя бы одной оси «Вход в задачу» (Impact/Uncertainty/Complexity) — слова владельца дословно, без причёсывания, в `workspace/<дата>-задача-<слаг>.md`, не редактируется впоследствии. Добавлено в `HOME.md` («Вход в задачу») и `LIFECYCLE.md`.
2. **Независимая проверка результата против этого файла**, не против собственных промежуточных решений Engineer — добавлено как пункт чек-листа `repository-design` и в `LIFECYCLE.md`. Честно отмечен нерешённый остаток: проверка выполняется тем же Engineer, который проектировал решение, не свежим независимым контекстом — настоящая независимость проверяющего не достигнута этим внедрением, только сверка с правильным объектом сравнения.
Обоснование: оба принципа признаны владельцем универсальными, не особенностью одного репозитория — второй сходится с уже трижды подтверждённым Exit Gate/Capability 5 (producer≠reviewer), первый — необходимое условие, чтобы второй имел, с чем сравнивать.
Отклонённые альтернативы: внедрить все пять кандидатов сразу — отклонено явно владельцем; A##/D## отдельно отклонён как однажды подтверждённая, не сходящаяся независимо находка (сам источник рекомендовал сначала проверить); маркеры для `update.sh` отклонены как решение уровня Foundation, не в полномочиях Engineer сейчас.
Источник: разбор `nick-vels/skills`, 2026-08-12; решение владельца, 2026-08-12.

### 2026-08-12 — Engineering-ревизия Researcher: Дом достроен, Skill упакован, Craft не тронут

Статус: Locked
Что решается: первый реальный прогон lifecycle на живом, ранее созданном агенте (не на самом Engineer). Достроена Foundation-структура (`VISION`/`HOME`/`AGENTS`/`SOUL`/`PROFILE`/`MEMORY`/`ROUTING`/`OPEN_QUESTIONS`/`COMMUNICATION`/`instructions`/`workspace`), `UPDATE.md`/`update.sh` перенесены сразу в исправленном виде (не унаследовали дефект, найденный на Engineer). `skills/reverse-engineering/` упакован в `SKILL.md`+`references/` без изменения содержания методологии (сверено контрольными суммами), зарегистрирован в Skill System как новый Skill (`origin: own`, категория `RESEARCH`, `DRAFT`). `permissions`-файлы очищены от мёртвых одноразовых записей. `.mcp.json` НЕ исправлен — нет проверенных данных для корректной записи `firecrawl`/`exa`, оставлено как честно незакрытое расхождение. `DECISIONS.md` заведён с форматом, вопрос об инлайн-решениях `REVIEW_LOOP.md` вынесен как `Proposal`, не решён за Researcher. `CLAUDE.md` исправлен (вёл мимо нового `HOME.md`) — найдено только повторной проверкой, не в исходном плане.
Обоснование: строго Engineering, не Craft — граница выдержана: ни строки в `MISSION.md`, методологии `reverse-engineering`, `knowledge/`, `research/`. Полный отчёт с разделением «что изменилось / что нетронуто осознанно / что не удалось / новые расхождения» — `workspace/2026-08-12-engineering-revision-report.md`.
Отклонённые альтернативы: исправить `.mcp.json` по аналогии с наилучшим предположением о командах запуска — отклонено, риск выдумать неработающую конфигурацию хуже честно оставленного разрыва. Перенести инлайн-решения `REVIEW_LOOP.md` в `DECISIONS.md` — отклонено явно владельцем при утверждении плана.
Источник: `workspace/2026-08-12-задача-researcher-remediation.md` (исходная задача, зафиксирована дословно); `workspace/2026-08-12-engineering-revision-report.md` (полный отчёт); решение владельца об утверждении Engineering-плана, 2026-08-12.

### 2026-08-12 — Предложение: минимальный механизм постоянного взаимодействия Engineer ↔ Researcher

Статус: Proposal
Что решается: первый настоящий прогон физического канала (Engineer сама запустила Researcher, проверила ответ, сама отправила уточнение по самостоятельно найденному Researcher пробелу, получила второй ответ) завершён успешно. Предлагаемый минимальный механизм, ничего сверх уже проверенного сегодня:
1. **Вызов**: Engineer порождает `general-purpose` Agent с обязательным префиксом контракта в промпте — прочитать устав целевого агента (`CLAUDE.md`→`HOME.md`→`MISSION.md`/аналог) прежде чем делать что-либо, затем сам research brief в уже существующем формате `OPEN_QUESTIONS.md` (задача/зачем/вопросы/критерий достаточности/ограничения/формат).
2. **Проверка достаточности** — делает Engineer, против зафиксированного до запроса критерия, не сам воркер. Совпадает с реальной практикой (Claude Code и Anthropic оставляют это на суждение вызывающей стороны — недедуктивный гейт есть, но опционален везде, где найден).
3. **Уточнение** — `SendMessage` на тот же `agentId`/имя с конкретно названным пробелом («по X недостаточно, уточни X»), не пересказ задачи с нуля. Проверено сегодня дважды, работает — резюмирует с полной историей.
4. **Эскалация владельцу** — только когда Engineer дошёл до решения, требующего его слова (Locked-переход, высокий радиус поражения, граница Craft vs Engineering) — не для самого исследовательского цикла.
5. **Персистентность** — воркер пишет находки в реальные файлы целевого агента (`knowledge/`, `memory/`, `OPEN_QUESTIONS.md`) по его собственным правилам роста знания — не в промежуточный формат Engineer.
Обоснование: не изобретено — узнаваемая комбинация уже существующих механизмов (`Agent`, `SendMessage`, уже принятый принцип «критерий до поиска»). Ближайший рыночный аналог (cross-session messaging) требует пересказывать контекст заново на каждое уточнение; найденная сегодня комбинация (файловая укоренённость + resume-с-историей) сильнее любого одного паттерна из разбора и не требует новой инфраструктуры.
Отклонённые альтернативы: копировать subagent-resume буквально — отклонено, механизм привязан к дереву субагентов одной сессии, Engineer и Researcher — разные репозитории/продукты. Копировать баланс OpenAI SDK/CrewAI/LangGraph (общий runtime, `Command`-объекты, coworker-tool-calls) — отклонено, требует общего процесса, которого у двух раздельных репозиториев нет.
Источник: `OPEN_QUESTIONS.md`, вопрос закрыт 2026-08-12 (5 систем, включая AutoGen Swarm через реальный повторный вызов); два реальных прогона канала этой сессии.

Примечание по статусу: **исправлена собственная ошибка Engineer**. Ранее эта запись была переведена в `Locked`, приняв формулировку-пересказ владельца («Engineer и Researcher взаимодействуют напрямую...») за подтверждающее слово — ровно то «выводить Locked из тона/пересказа», что правило этого файла прямо запрещает. Владелец явно поправил: правило прямого взаимодействия остаётся `Proposal` до его отдельного явного решения, независимо от того, что физический механизм уже дважды подтверждён практикой. Два реальных эмпирических прогона (запуск, проверка достаточности, уточнение, resume, evidence на каждом шаге) — сильное основание для будущего перехода в `Locked`, но не сам переход; правила это не заменяет.

### 2026-08-12 — Эталон обязанностей Engineer при сборке capability-набора агента (6 пунктов)

Статус: Locked
Что решается: на основании двух независимо проверенных исследований Researcher (различение слоёв доступности; реальные механизмы выбора/маршрутизации capabilities и моделей) зафиксирован проверочный список того, что Engineer обязан уметь при сборке любого агента — не «дать максимум современного», а:
1. Найти — что вообще существует под профессию агента (Skills + MCP + модели), не только Skills.
2. Разложить по слоям — для каждого кандидата отдельно: доступно технически / принадлежит агенту / разрешено / подходит для задачи — не одним суждением (прямая причина сегодняшней ошибки с Exa).
3. Собрать минимальный набор под профессию, не максимальный.
4. На каждой конкретной задаче заново решать — дешёвый путь или дорогой, эскалировать только когда дешёвого не хватило.
5. Проверять после использования, что выбранное реально дало результат — честно понимая, что полностью автоматической проверки нигде не нашли, человек/LLM-judge с калибровкой остаётся всегда.
6. Периодически пересматривать по симптому (не по расписанию — расписание-driven механизма не нашли нигде) — не появилось ли сильнее/дешевле.
Обоснование: «максимально упакованный агент» и «оптимально подобранный агент» — разные, несовместимые цели; доказательства (независимый замер параллельного fan-out — 2.6–5.9x дороже без выигрыша в скорости; ни одна найденная система не гейтит использование избыточной подготовкой) на стороне второй. Список — эталон, против которого дальше проверяется существующая Skill System и любые Skills Engineer, не наоборот (сначала список, потом ревизия системы под него).
Отклонённые альтернативы: «дать каждому агенту все современные инструменты» — отклонено явно владельцем, ведёт к раздутому агенту без доказанного выигрыша. Начинать сразу с вопроса «нужны ли все Skills» — отклонено, подменяет эталон способностей оптимизацией уже существующего набора и рискует не заметить отсутствующую способность.
Источник: `OPEN_QUESTIONS.md`, оба вопроса закрыты 2026-08-12 (слои доступности; production capability/model routing); формулировка эталона — обсуждение с владельцем, тот же день.

### 2026-08-12 — M1 закрыт: независимый Reviewer против зафиксированного критерия достаточности

Статус: Locked
Что решается: закрытие честно названного GAP в `LIFECYCLE.md` (§3, «Независимая проверка результата против исходной задачи») — проверка выполнялась тем же Engineer, который проектировал решение, не независимым контекстом. Найдено и подтверждено как отсутствующая способность (M1) в аудите capability-map Engineer (Researcher, 17→13 capabilities + 4 gaps, категория «Инженерия»). Механизм: по завершении сборки/доработки агента отдельный, свежий Agent получает только зафиксированный дословно исходный текст задачи + заранее (до проверки) зафиксированный критерий достаточности «что значит профессиональный результат» + фактический результат агента; не получает план/решения/промежуточные документы Engineer. Проверяет соответствие критерию — да/нет с причиной, не «на вкус». Поправка владельца при утверждении: не «судит по одному вопросу», а именно «против заранее зафиксированного критерия» — иначе Reviewer тоже мог бы судить произвольно.
Обоснование: третье независимое схождение одного и того же принципа (Exit Gate Researcher 2026-08-11; Capability 5 producer≠reviewer; nick-vels G2/G4 blind acceptance) плюс подтверждение мировой практикой (LLM-as-judge с калибровкой, Agent-as-a-Judge — нигде не найдено полностью автономного решения, человек в калибровке остаётся). Реальный evidence уже есть, что такой Reviewer мог бы поймать: сегодняшние ошибки с README и статусами Skills.
Отклонённые альтернативы: сразу оформить как отдельный Skill — отклонено явно владельцем; сначала механизм проверяется на реальной сборке, только потом, если оправдает себя, оформляется в Capability/Skill (capability ≠ Skill, не торопить упаковку).
Источник: `AI Intelligence/knowledge/evidence/2026-08-12-engineer-capability-map-audit.md` (M1); `LIFECYCLE.md`, «Независимый Reviewer»; обсуждение и утверждение с владельцем, 2026-08-12.

### 2026-08-12 — Technical Execution Record: task_id → session_id → execution trace, без logging-инфраструктуры

Статус: Locked
Что решается: как Engineer механически восстанавливает связь «задача → сессия агента → фактический execution trace» без самоотчёта агента и без построения отдельной платформы. Проверено экспериментом дважды независимо: (1) харнесс возвращает `session_id` напрямую в результате запуска (`claude -p ... --output-format json`); (2) уникальный `task_id`, вложенный Engineer в задачу при запуске, однозначно находит ту же сессию поиском по содержимому файлов, без знания `session_id` заранее. Правило: каждый запуск агента получает уникальный `task_id`; Engineer сохраняет возвращённый `session_id`; execution trace — источник технического evidence, самоотчёт агента им не является. Относится ко всем агентам, не только Researcher.
Обоснование: закрывает честно названный ранее вопрос из исследования Researcher про observability долгоживущих агентов («ID должен прийти извне») — оказалось, что для нашего реального случая (одна машина) он приходит извне бесплатно, автоматически, механизм уже существует в среде и не требует строительства. Не смешивается с отдельной находкой того же эксперимента — независимый запуск агента имеет фиксированную стоимость входа (~$0.19 на тривиальную задачу, из-за загрузки полного контекста Foundation) — это отдельная, следующая инженерная задача про стоимость, не часть этого решения (`knowledge/cost-observations.md`).
Отклонённые альтернативы: обязательная запись `session_id` в память/дневник агента вручную — отклонено явно владельцем: агент снова становится источником истины о собственном runtime, а не сам runtime; риск забыть шаг. Отдельный logging-сервис (LangSmith-подобный) — отклонено, инфраструктуры для этого нет и без неё уже работает для нашего случая.
Источник: `OPEN_QUESTIONS.md`, вопрос закрыт 2026-08-12 («Как production-системы обеспечивают observability... для самостоятельно живущих агентов»); прямой эксперимент Engineer в этом же диалоге (`claude -p` в директории AI Intelligence, session_id `d98b5ab9-82c9-4db4-8ecd-2e8918e860ef`, найден также по `task_id ENG-2026-08-12-SESSIONID-TEST-001`).

### 2026-08-12 — Финальное инженерное принятие capability-аудита: 13 способностей + 4 gaps

Статус: Locked
Что решается: после аудита Researcher (17→13 capability + M1–M4) и закрытого point-fix (`webapp-testing` vs `VISION.md`) — инженерное решение по каждой позиции, не повторный пересказ находок.

**Сделано сейчас, механически, без нового дизайна** (решение уже было очевидным из отчёта, не требовало обсуждения):
- Capability 9 (Build Quality Discipline) — `test-driven-development`, `systematic-debugging`, `verification-before-completion` установлены как есть (Researcher: «готово и проверено рынком, просто не подключено»). `HOME.md` синхронизирован.
- Capability 3 (`tool-selection`) — STRENGTHEN выполнен: исправлена фактическая ошибка про недоступность Exa/Firecrawl/Context7 (уже подтверждено `claude mcp list` ранее тем же днём), внесена явная 4-слойная модель (доступно/принадлежит/разрешено/подходит), добавлена находка про `ToolSearch` с закрытым запросом вместо открытого.
- Capability 13 (`webapp-testing`) — REMOVE подтверждён (point-fix против `VISION.md`, вердикт не изменился). Файла нет и не было — ничего физически удалять не нужно.

**Выполнено отдельным, следующим шагом того же дня (2026-08-12, позже)** — авторство доведено до конца, не только решение:
- Capability 4 (`capability-creation-methodology`) — REBUILD выполнен и установлен: методология (domain discovery, Legacy-first Alarm, обязательный Open Source Research, take/reject-лог) сохранена целиком, ссылки на `idea-calibration.md`/Bootstrap Sequence заменены на реальные точки Foundation (`HOME.md` «Вход в задачу», `workspace/<дата>-задача-*.md`, формат `DECISIONS.md` для Фазы 8).
- Capability 5 (`architecture-review`) — REBUILD выполнен и установлен: Capabilities map / Reliability Architecture / AI Reasoning Boundaries перенесены без изменения содержания, чужие ссылки убраны. Capabilities map теперь явно требует Skills+MCP+модели — частично закрывает M3.

**Решено, но реальное авторство отложено отдельным шагом** — требуют адаптации инфраструктуры/скриптов (`eval-viewer/generate_review.py`, `.superpowers/sdd/`-workspace, `sdd-workspace`/`task-brief`/`review-package` скрипты), которой в нашей среде нет; переносить вслепую — риск бегло сделанного авторства, не решение:
- Capability 6 (`skill-creator`+`writing-skills`) — MERGE в «авторство и тестирование Skill», требует своей адаптации eval-инфраструктуры.
- Capability 7 (`dispatching-parallel-agents`+`executing-plans`+`subagent-driven-development`) — MERGE в «делегированное исполнение», требует своей адаптации ledger/review-loop инфраструктуры.
- Capability 8 (Model/cost tiering) — выносится отдельной способностью из Capability 7, тот же блокер: сначала нужен разбор Capability 7.

**Оставлено в резерве, не устанавливается — нет текущей потребности**: Capability 10 (`claude-api` reference), Capability 11 (`claude-api` migrate, частично закрывает M2), Capability 12 (`mcp-builder`) — нет `.mcp.json`, нет кода на raw API.

**MISSING — подтверждены, распределены по приоритету, не решены все сразу**:
- M1 — закрыт отдельным Locked-решением тем же днём (Независимый Reviewer + `TECHNICAL_PASSPORT.md`).
- M3 — высокий приоритет: прямо противоречит собственному Locked «Эталону обязанностей Engineer», пункт 1 (Skills+MCP+модели, не только Skills). Не решён — следующий кандидат на отдельный разбор, по образцу M1.
- M2, M4 — подтверждены, без домашней прописки, не решены. В мире нет расписание-driven аналога M2; M4 — известный, ранее отложенный пробел (`DECISIONS.md`, «Три observations…»).
Обоснование: разделение «что можно сделать механически прямо сейчас» и «что требует реального авторства» — не откладывание ради откладывания: авторство Skill — содержательная работа, которая заслуживает отдельного внимания, не бандла из шести решений за один проход, тот же принцип, что уже применялся к M1 («сначала проверяется на реальной сборке, не сразу оформляется»).
Отклонённые альтернативы: авторить все MERGE/REBUILD Skills в этом же проходе — отклонено, риск сделать это бегло; устанавливать capability 10–12 «на будущее про запас» — отклонено, не по правилу «ставим по потребности, не максимально» (Эталон, пункт 3).
Источник: `AI Intelligence/knowledge/evidence/2026-08-12-engineer-capability-map-audit.md`; `OPEN_QUESTIONS.md` (аудит 17→13, закрыт); `HOME.md`, `skills/tool-selection/SKILL.md`, `skills/test-driven-development|systematic-debugging|verification-before-completion/` — изменения этого решения.

### 2026-08-12 — Три решения по итогам первого теста Technical Passport на себе

Статус: Locked
Что решается:
1. **`skill-authoring` не считается готовым** — статус `DRAFT / needs verification`, не `VERIFIED`. Структурно хорош (один связный цикл, не склейка), но: потерял живые примеры при сжатии источников; требует дорогого отдельного прогона через `Agent` без обсуждения этой стоимости внутри самого Skill; сам не прошёл собственный обязательный тест (Iron Law). Не переписывается целиком сейчас — сначала проверяется на одном реальном случае.
2. **Engineer обязан применять собственный Technical Passport и его же дисциплину к собственной работе, не только к работе других агентов.** Конкретно здесь: `skill-authoring` требует «нет Skill без проваленного теста сначала» — Engineer создала его, не выполнив это же требование к нему самому. Это не решается обсуждением — недостающий тест должен быть проведён, не отложен.
3. **`task_id` создаётся в момент, когда Engineer признаёт работу отдельной задачей — до первого действия именно этой задачи, не позже.** Работа, сделанная до этого момента (даже если по факту относится к теме), — подготовительный контекст, не часть execution trace этой задачи; притворяться иначе — нечестно по отношению к самому методу. Явно отклонено усложнение (поиск по именам файлов/темам/времени в дополнение к `task_id`) — риск сделать корреляцию хрупкой, не понятной. Простое правило важнее точности в редком краевом случае.
Обоснование: первый реальный прогон механизма Passport+self-check+trace на самой Engineer (`workspace/agent-runs/ENG-2026-08-12-SKILLMERGE-001.md`) нашёл настоящее расхождение (self-check заявил 2 Read источников, trace показал 1) — причина оказалась не во лжи, а в границе метода (task_id создан после части работы). Это и есть та проверка, ради которой механизм строился.
Отклонённые альтернативы: сразу переписать `skill-authoring` целиком по итогам разбора — отклонено, чинить не проверив на реальном случае — тот же риск, от которого предостерегали раньми (`DECISIONS.md`, «M1 закрыт»); усложнить корреляцию доп. признаками — отклонено явно владельцем.
Источник: `workspace/agent-runs/ENG-2026-08-12-SKILLMERGE-001.md`; обсуждение с владельцем, 2026-08-12.

### 2026-08-12 — `skill-authoring` поднят до `VERIFIED`, с наблюдением

Статус: Locked
Что решается: по итогам реального RED/GREEN теста (`workspace/agent-runs/ENG-2026-08-12-SKILLTEST-002.md`) владелец одобрил повышение статуса `skill-authoring` с `DRAFT/needs verification` до `VERIFIED`. Явное наблюдение, зафиксированное вместе с одобрением, не отдельно: протестированы напрямую только 3 из 6 типов commit-сообщений в тестовом сценарии (feat/docs/refactor, не fix/test/chore) — частичное покрытие принято как достаточное для этого уровня статуса, но не как основание подниматься выше без дополнительной проверки.
Обоснование: разница в поведении реальная, не постановочная — один и тот же фрейминг «спешим» под давлением дал 0 из 3 верных форматов без Skill и 3 из 3 с ним, включая то, что with-skill агент сам провёл RED прежде чем писать Skill, и сам исправил найденный по ходу недочёт (description пересказывал правило вместо триггера).
Отклонённые альтернативы: сразу VERIFIED·LIVE — отклонено, usage count равен нулю, реального использования в продуктивной задаче ещё не было.
Источник: `workspace/agent-runs/ENG-2026-08-12-SKILLTEST-002.md`; `HOME.md`, строка `skill-authoring`.

### 2026-08-13 — Синхронизация Skill System: закрыт разрыв между тем, что построено у Engineer, и тем, что лежит в общей библиотеке

Статус: Locked
Что решается: с 2026-08-12 три Skills (`architecture-review`, `capability-creation-methodology`, `skill-authoring`) были пересобраны/созданы только локально у Engineer — Skill System хранила старые, частью ломаные версии (ссылки на несуществующие в Foundation `idea-calibration.md`/Bootstrap Sequence), а решения REMOVE (`webapp-testing`) и MERGE (`skill-creator`+`writing-skills` → `skill-authoring`) из капабилити-аудита 2026-08-12 не были исполнены на уровне библиотеки вообще. Владелец назвала это прямо: «Skill System должен быть помощником и опорой, а сейчас это хромая утка» — сделано:
1. `architecture-review` и `capability-creation-methodology` — рабочие версии Engineer скопированы в Skill System поверх старых, `evidence.json` дополнен (не переписан) записью об обновлении, ссылки на `idea-calibration` убраны из карточек (`card.related`).
2. `skill-authoring` — зарегистрирован как новый Skill в Skill System (`set-origin --type own`, `set-category ENGINEERING`), с карточкой, указывающей на два источника.
3. `skill-creator` и `writing-skills` — помечены `DEPRECATED` (`skillctl.py deprecate`, файлы и история сохранены, не удалены) с причиной «заменены skill-authoring».
4. `webapp-testing` — помечен `DEPRECATED` с причиной уже принятого REMOVE-решения 2026-08-12.
5. `tool-selection` — синхронизирован (сегодняшний раздел Governance по инструменту/модели теперь и в общей библиотеке, не только локально).
6. `registry.json`/`index.html`/`LIBRARY.md` пересобраны (`skillctl.py registry/explorer/library`).
Честно не сделано в этом же проходе: Capability 6-7-8 (объединение `dispatching-parallel-agents`+`executing-plans`+`subagent-driven-development`, model/cost tiering) — не тронуты, это не синхронизация уже готового, а отдельное, не начатое авторство (требует адаптации ledger/review-loop инфраструктуры, которой нет), тот же осознанный отказ бандлить, что уже был 2026-08-12.
Обоснование: разрыв не гипотетический — Engineer улучшала Skills у себя минимум три раза (`skill-authoring`, `architecture-review`, `capability-creation-methodology`) и ни разу не закрывала цикл обратно в общую библиотеку; Skill System из «источника, откуда берут» стал «складом устаревших копий того, чем реально пользуется Engineer» — прямая угроза самой цели системы (SKILL-SYSTEM.md: библиотека, которой можно доверять).
Отклонённые альтернативы: заодно доавторить Capability 6-7-8 «пока всё равно чиним Skill System» — отклонено, это отдельная содержательная работа авторства, не синхронизация уже решённого (тот же принцип разделения из 2026-08-12).
Источник: `Skill System/skills/{architecture-review,capability-creation-methodology,skill-authoring,skill-creator,writing-skills,webapp-testing,tool-selection}/evidence.json`, изменения 2026-08-13; решение владельца, 2026-08-13.

### 2026-08-13 — Новый Skill `idea-calibration`: объединение лёгкой версии и полного протокола AI_OS

Статус: Locked
Что решается: владелец потребовал пересмотреть вердикт «отклонено» по `idea-calibration` из аудита AI_OS — сравнить со своей версией и объединить лучшее, а не просто отклонить как «уже покрыто». Сравнение: своя лёгкая версия (`HOME.md`, «Вход в задачу») — три оси, для high-случая просто 2-3 вопроса. У AI_OS — тот же вход, но дальше: точная формулировка проблемы, обязательная глубина внешнего поиска для крупных решений, три альтернативы с trade-offs, явный аргумент против собственного решения (Routing Falsification), сверка с исходной формулировкой перед принятием решения (не только после сборки).
Решение: лёгкая версия остаётся как есть для всех обычных случаев — не заменяется. Добавлен отдельный Skill `idea-calibration`, подключаемый только когда решение конкретно крупное (новый агент, смена методологии, редизайн больше двух модулей) — три альтернативы, trade-offs, Routing Falsification, Problem Fidelity до решения.
Обоснование: у AI_OS отсутствие именно этих элементов (три альтернативы, явный аргумент против) реально стоило переделки (Legacy-first Alarm пропущен дважды подряд в одной сессии) — не гипотетическая находка.
Отклонённые альтернативы: полностью заменить лёгкую версию полным протоколом — отклонено, для обычных задач это была бы лишняя бюрократия без доказанной необходимости.
Источник: прямое требование владельца пересмотреть вердикт по `idea-calibration`, 2026-08-13; `AI_OS/skills/idea-calibration.md`.

### 2026-08-13 — `legacy-repository-audit` переписан: главный принцип «исследование ≠ выбор», обязательная проверка «уже есть у нас?»

Статус: Locked
Что решается: владелец назвала точную причину двух ошибок этой сессии (ложная классификация «не инженерное» без проверки регистра; пять забытых находок) — Engineer незаметно для себя превращает «исследовала» в «решила», без прохождения через владельца. Правильный цикл: исследовала → полный инвентарный список в файле → для каждой строки реально проверила «уже есть у нас?» → классифицировала (забираем: да/нет/UNKNOWN) → показала владельцу целиком → владелец решил → внедрила.
`skills/legacy-repository-audit/SKILL.md` переписан целиком: таблица находок теперь — Найдено / Что это / Уже есть у нас? / Где должно жить / Забираем? / Почему; шаг «уже есть у нас?» — отдельный обязательный подшаг с реальной проверкой (не по памяти, не по домену находки); UNKNOWN — равноправный статус, не повод выбросить строку.
Обоснование: обе ошибки этой сессии — не случайность процесса, а его структурная дыра (решение принималось до показа владельцу). Правило меняет саму методологию Engineer — высокий радиус поражения, но исполнено сразу по прямому, явному слову владельца (описавшей проблему и решение целиком) — независимая проверка не проводилась отдельным ходом, риск принят явно тем же способом, что уже применялся раньше в этой сессии.
Источник: прямой разбор владельца, 2026-08-13 («исследование источника ≠ выбор того, что забрать»).

### 2026-08-13 — Механическое лечение «забыла находку»: список — в файл, не в память диалога

Статус: Locked
Что решается: владелец прямо спросила, как лечится то, что Engineer может забыть находку (пять пунктов из анализа AI_OS выпали при исполнении без объяснения). Ответ — не «стараться внимательнее», это не механический фикс и не проверяется. Механический фикс: список находок фиксируется в файле в момент формирования (`workspace/<дата>-legacy-audit-*.md`); при исполнении позже — вычёркивание по файлу, не по памяти разговора. Тот же принцип, что уже применяется к исходной задаче владельца (`workspace/<дата>-задача-*.md`, не редактируется, сверяется заново) — распространён на список находок собственного анализа Engineer.
Внесено в `skills/legacy-repository-audit/SKILL.md` (Шаг 4, Шаг 6, чек-лист) — тот же Skill, что и был построен по прямой просьбе владельца «построй правило исследования репозиториев», усилен сразу же, не отдельным будущим шагом.
Обоснование: причина пропуска — не невнимательность как черта, а структурная: разговор длинный, список только проговорен, не записан — то же самое структурное объяснение, что уже подтверждалось в этой сессии для task_id (память диалога ненадёжна как хранилище через границу времени/реплик).
Источник: прямой вопрос владельца «как это должно лечиться???», 2026-08-13.

### 2026-08-13 — Собственная ошибка учёта находок AI_OS исправлена; новый Skill `legacy-repository-audit`

Статус: Locked
Что решается: два реальных дефекта в разборе AI_OS, найденные владельцем прямым вопросом «что ещё не взяла и почему», а не собственной проверкой Engineer:
1. **Ложная классификация**: заявила, что `conversion-method`, `taste-capture`, `opportunity-discovery` — «не инженерия, остаётся владельцу», хотя проверка `registry.json` показала — все три (и `youtube-analysis`) уже в Skill System с 10 августа, задолго до этой сессии. Ошибка — не сверилась с реальным регистром перед ответом, дала оценку по памяти/предположению.
2. **Незаконченный учёт**: пять находок (Outcome Gate, Лестница обобщения, порядок «бизнес-практика→агенты→Skills→MCP→платформа», разведение Input/Constraint, полный 6-вопросный Final Quality Gate) были названы ценными в собственном анализе, но не вошли в фактически исполненный список из 4 пунктов без единого слова объяснения.
Исправление: составлен и показан владельцу полный, честный список — что уже было (ошибочно не узнано), что реально пропущено и почему (забыла, не отклонила осознанно), что представляет собой отдельная техника (Business Assets lifecycle/Platform-Client), а не готовое правило. Ничего из пропущенного не добавлено без отдельного подтверждения владельца — список показан, решение о добавлении не принято этим же ходом.
Создан новый Skill `legacy-repository-audit` — формализует процесс проверки старого/внешнего репозитория (клонировать read-only → идентификация отдельным сообщением → глубокое чтение по категориям → классификация каждой находки без исключений, явно один из вариантов → сверка с реальным состоянием системы, не по памяти → показ полной таблицы → исполнение только после подтверждения). Собран по итогам двух реальных прогонов (MAIN_ENGINEER, AI_OS) — оба раза процесс придумывался заново, второй прогон вскрыл ошибку первого.
Обоснование: оба дефекта — не гипотетические, найдены на практике в этой же сессии; Skill предотвращает именно то, что уже случилось дважды (неполный учёт находок), а не абстрактную заботу о процессе.
Отклонённые альтернативы: молча исправить ошибку без явного признания — отклонено, честность про собственную ошибку важнее гладкого отчёта.
Источник: прямой вопрос владельца «что ещё ты не взяла?? почему??», 2026-08-13; `Skill System/registry.json`, прямая проверка.

### 2026-08-13 — Инженерная ревизия AI_OS: разделена на две части, инженерная передана Engineer

Статус: Locked
Что решается: AI_OS (`github.com/megapolisagent/AI_OS`) — прямой преемник MAIN_ENGINEER+MAIN_ASSISTANT (мигрировали туда), сама остановлена владельцем ~28 июля, до старта нынешней связки ДОМ/Skill System/Researcher/Engineer. Владелец решил разделить её на две части: инженерная — полностью передана Engineer; вторая (роль «AO» — отслеживание бизнес-целей/пробелов способностей, маршрутизация к специалистам, Capability Map по жизни и бизнесу владельца, готовые бизнес-агенты — Маркетолог, Агент лендингов, Коуч) — остаётся у владельца на обдумывание (возможно, «Продюсер» или другая роль), не инженерная и не тронута.
Из инженерной части внесено:
1. `capability-creation-methodology` — Legacy-first Alarm расширен: поиск по всему релевантному проекту, не только по уже упомянутым папкам. Найдено на практике AI_OS: тот же пропуск случился дважды подряд в одной сессии именно из-за узкого поиска.
2. `skill-authoring` — два новых обязательных пункта чек-листа: явное «когда НЕ использовать» и явное «что получает следующий потребитель результата». Оба — реальные пробелы, не гипотетические (AI_OS требует явный ответ на 7 вопросов для каждого Skill, у нас требовались не все).
3. `architecture-review` — принцип Governance × Craft: правильный процесс и качество самой работы — разные оси, одна не заменяет другую.
4. `HOME.md` — короткая проверка перед тем, как считать ответ готовым (Coverage/Evidence/Simplification/Future Problems), сжатая версия Final Quality Gate из AI_OS.
Не взято (сознательно, принадлежит второй части): Business Assets lifecycle (missing→current→stale→archived), разделение Платформа/Конфигурация клиента, Responsibility Contract бизнес-агента, Input/Constraint для capability map, сами специализации (Маркетолог, Агент лендингов, Коуч) — предметная методология бизнеса, Craft, не Engineering.
Обоснование: то же правило, что и с MAIN_ENGINEER и Skill System сегодня — брать проверенное на практике, не всё найденное; граница инженерное/не инженерное проведена владельцем явно, не Engineer.
Отклонённые альтернативы: сохранить AI_OS как отдельный работающий слой над Engineer — отклонено, система остановлена и не действующая, слой над пустотой не имеет смысла; забрать всё найденное сразу, включая бизнес-паттерны — отклонено, не инженерная часть, не решение Engineer.
Источник: полное чтение репозитория AI_OS, 2026-08-13; решение владельца о разделении на две части, 2026-08-13.

### 2026-08-13 — Итог ревизии старого MAIN_ENGINEER: 4 усиления Engineer внесены, 3 механизма явно не взяты

Статус: Locked
Что решается: по итогам полной инженерной ревизии предыдущей версии Engineer (`github.com/megapolisagent/MAIN_ENGINEER`, 14 файлов) и внешней проверки Researcher трёх спорных механизмов (`OPEN_QUESTIONS.md`, закрыт 2026-08-13) — владелец принял решение по каждой находке. Внесено сейчас, механически:
1. **Capability map архитектурной фазы расширена с 5 до 7 измерений** — добавлены «инфраструктура» и «браузер-автоматизация» (`skills/architecture-review/SKILL.md`). Честно: 7-осевого стандарта индустрии внешне не нашли — расширение принято как собственное инженерное решение, не проверенная чужая практика.
2. **Чек-лист «агент готов к реальной работе»** — новый раздел `skills/repository-design/SKILL.md`, отдельный от чек-листа «репозиторий готов» (файлы на месте) и от Skill-lifecycle (проверен конкретный Skill). Пять пунктов (Working Loop на реальных данных, повторяемость, explainability, обратная связь, стабильная детерминированная база перед AI-слоем), переформулированы в терминах уже существующих понятий Engineer, не скопированы как чужая терминология.
3. **Стоп-условие** — новое правило в `HOME.md`: один и тот же сбой повторился несколько раз или исправление стало костылём поверх костыля → остановиться, предложить владельцу откат/упрощение/пересборку, не чинить дальше молча.
4. **Governance по инструменту/модели** — новый раздел `skills/tool-selection/SKILL.md`: для каждого реального инструмента (WebSearch/WebFetch/Exa/Firecrawl/Context7/Bash/Agent/модель субагента) — когда использовать, когда нет, ограничения, альтернатива. Закрывает практический разрыв, который уже дал реальную ошибку сегодня (Exa была доступна, но не рассматривалась).
5. **Длина файла как сигнал делить** — правило в `HOME.md` + уточнение в `skill-authoring/SKILL.md`: порог 500 строк для `SKILL.md` подтверждён первоисточником (офиц. best-practices Anthropic, не «~300», как было у старого MAIN_ENGINEER, и не догадка) — для остальных файлов устава точного числа нет, применяется тот же принцип без выдуманной цифры.

**Явно не взято** (решение владельца, не забыто и не отложено молча):
- **Tier-протокол записи между равноправными агентами** (Engineer↔Researcher) — Researcher не нашла совпадающей внешней практики (частичные аналоги закрывают только одну ось паттерна каждый). Остаётся собственным экспериментальным механизмом старого MAIN_ENGINEER, доказанным только его же двумя прогонами — не превращается в обязательный стандарт Engineer сейчас, не выбрасывается из рассмотрения на будущее.
- **«Business Loop > Engineering Loop»** — подтверждён только общий предок (Lean Startup, retention-first у a16z/Sequoia), AI-agent-специфичная версия правила внешне не найдена. Остаётся как наблюдение, не правило: эта многодневная сессия — почти сплошь Engineering Loop, а агента, построенного и реально работающего с нуля, до сих пор нет ни одного. Сигнал зафиксирован здесь текстом, отдельного механизма/чек-листа под него не создаётся.
- **Паспорт-заголовок внутри файла** (Версия/Дата/Тип/Статус/Связанные файлы как поле в самом файле) — не подтверждён и не опровергнут ни одним первоисточником. Не внедряется. Явно: нельзя говорить «Anthropic требует Technical Passport» — не требует, это собственная конструкция Engineer, никак не связанная с этой непроверенной идеей.
Обоснование: разделение «что взять» реально основано на разной силе доказательств по каждому пункту, не на предпочтении — где Researcher подтвердила первоисточником (500 строк, versioning для prompt/config-файлов), решение принято уверенно; где не подтвердила (Tier-протокол, Business Loop, паспорт-заголовок), решение — не выбросить, а честно понизить статус до наблюдения/эксперимента, не выдавать неподтверждённое за проверенную практику.
Отклонённые альтернативы: внедрить все находки сразу, включая непроверенные — отклонено явно владельцем. Выбросить непроверенные механизмы совсем — отклонено явно владельцем («я бы его не выбрасывал, но и не превращал сейчас в обязательный стандарт»). Оформить это отдельным новым Skill/системой поверх существующих Skills — отклонено явно владельцем: «хороший момент остановиться... не создавать очередную систему вокруг системы».
Источник: `OPEN_QUESTIONS.md`, вопрос закрыт 2026-08-13; решение владельца, 2026-08-13; правки этим решением — `skills/architecture-review/SKILL.md`, `skills/repository-design/SKILL.md`, `skills/tool-selection/SKILL.md`, `skills/skill-authoring/SKILL.md`, `HOME.md`.

### 2026-08-12 — Три observations от внешней сверки Researcher: не вносить в `repository-design` сейчас

Статус: Locked
Что решается: `knowledge/repository-design-observations.md` содержит 3 находки Researcher (least-privilege/allowed-tools, воспроизводимый eval, producer≠reviewer independence), каждая с источником. Решение владельца: не вносить их в `repository-design` сейчас, оставить как observations, `Proposal` не создавать.
Обоснование: Researcher доказала наличие blind spots чек-листа, но не доказала, что покрывать их должен именно `repository-design` — reproducible eval может принадлежать не Skill, а механизму Foundation; producer≠reviewer может быть routing/role-механизмом; least-privilege — отдельной security-проверкой. Присвоить находке архитектурное направление сейчас значило бы незаметно превратить исследовательский результат в решение, минуя отдельную проверку, какому носителю это принадлежит.
Отклонённые альтернативы: сразу оформить как `Proposal` «добавить три пункта в `repository-design`» — отклонено явно владельцем по причине выше.
Источник: `knowledge/repository-design-observations.md`, запись 2026-08-12; решение владельца, 2026-08-12.

### 2026-08-13 — Унаследован универсальный pre-task check из Дома

Статус: Locked
Что решается: `HOME.md` (новое правило в «Правила» + указатель внутри «Вход в задачу») и новый файл `instructions/pre-task-check.md` обновлены по универсальному решению Дома (см. `Foundation/04_REPOSITORY/DECISIONS.md`, 2026-08-13, «Универсальный pre-task check»). Для Engineer отдельного профессионального вопроса сверх базовых 8 не добавлено — уже существующий гейт «Вход в задачу» (три оси, workspace-фиксация исходной задачи дословно, `idea-calibration` для крупных решений) остаётся единственным механизмом определения порога; pre-task check наполняет его содержанием, не дублирует вторым механизмом.
Обоснование: решение принято на уровне Дома, применимо ко всем агентам без исключения. Восемь вопросов пересекаются по смыслу с уже действующей практикой Engineer (исходная задача дословно в `workspace/`, критерий достаточности до результата, независимый Reviewer для M1) — не новая методология, явное оформление уже частично работающей дисциплины в единый универсальный формат.
Источник: `Foundation/04_REPOSITORY/DECISIONS.md`, 2026-08-13; исследование Researcher о мировой практике предотвращения пяти сбоев автономных агентов, 2026-08-13.
