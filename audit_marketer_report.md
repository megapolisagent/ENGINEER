# Аудит AI Marketing Strategist (независимая проверка Codex) — 2026-09-02

Аудит покрыл собственные файлы агента (`HOME.md`, `SOUL.md`, `instructions/`, `skills/`,
`knowledge/`, `workspace/`, `.claude/`). `.venv/` и внешний референс-дамп `research/` (170+ МБ)
из полного построчного аудита исключены по объёму, оценены выборочно.

## 1. Оценка Craft & Behavioral Contract

- **Запрет на выдуманные цифры действует де-факто (через ЗНАЮ/ПРЕДПОЛАГАЮ/НЕ ЗНАЮ и обучение
  на реальном провале), но де-юре не зафиксирован как отдельная норма.** Нигде явно не написано:
  «конкретное число — бюджет, охват, конверсия, цена конкурента — без источника запрещено,
  допустим только диапазон с маркером ПРЕДПОЛАГАЮ». Прошлый реальный провал (числа от
  суммаризатора приняты за факт в матрице Avito v1) зафиксирован как feedback в памяти, но не
  поднят в `HOME.md` как обязательное правило.
- **Обязательного опросника перед стратегией нет** — осознанное решение («работает и на узкий
  вопрос, не навязывает структуру»), но риска не снимает: при неполном брифе агент может
  закрыть пробелы маркером ПРЕДПОЛАГАЮ вместо того, чтобы остановиться и спросить.
- **Штампов почти нет** в `SOUL.md` (чистый, живой тон, few-shot есть), но в `HOME.md` и
  особенно в `skills/marketing-strategy/SKILL.md` — инженерный жаргон («коммодитизирована»,
  «Capability Map», «ацикличен», «Responsibility-матрица»), который агент читает при старте и
  может воспроизвести в разговоре с владельцем — прямое нарушение собственного правила SOUL.md
  «термин объясняется тут же».
- Стандарты Data Contract/Behavioral Contract/Tooling Inventory из
  `ENGINEER/skills/repository-design/SKILL.md` в этом агенте нигде не упомянуты и не отражены —
  агент строился раньше, чем эти стандарты появились.

## 2. Оценка Tooling

| Операция | Реальный инструмент | Статус |
|---|---|---|
| Веб-поиск/веб-фетч | `mcp__exa__*`, `mcp__firecrawl__*`, `WebFetch` | Задокументирован |
| Публичный Telegram | `Bash` → `curl` на `t.me/s/<handle>` | Образцово задокументирован, с явным объяснением «почему не WebFetch» (зафиксированный инцидент с галлюцинацией суммаризатора) |
| Работа с docx/xlsx/pdf | Python через `Bash` + `.venv` | Механизм работает, но нигде не описан отдельным SKILL/instruction |
| Reviewer | Subagent, `model: haiku`, только Read/Grep/Glob | Задокументирован явно |
| Связь с другими агентами (Авитолог, ENGINEER) | Декларация в тексте («отдать итог в чат») | Нет реального механизма — не MCP, не API, просто текст |
| Avito/ЦИАН/Домклик как источник | Формально заблокирован в `settings.json` | Конфликтует с `settings.local.json` (см. CRITICAL C-2) |

## 3. Замечания

### CRITICAL
- **Нет явного запрета на числовые параметры без источника как отдельной нормы в `HOME.md`**
  — при уже случившемся реальном провале того же класса.
- **Конфликт разрешений `cian.ru`**: `settings.json` запрещает `WebFetch(domain:cian.ru)`,
  `settings.local.json` разрешает `WebFetch(domain:www.cian.ru)` — поведение непредсказуемо,
  зависит от порядка применения файлов.

### WARNING
- Двойная нумерация файлов в `knowledge/businesses/агентство-недвижимости-премиум/` (два `10_*`
  и два `11_*`), плюс параллельно существующий устаревший монолитный файл с тем же именем.
- `ROUTING.md` не обновлялся с 2026-08-19 — 15 новых knowledge-файлов не отражены, новая сессия
  не найдёт путь к материалам по Avito/застройщикам/TOV.
- `skills/marketing-strategy/SKILL.md` и `telegram-research` реально прошли рабочий цикл,
  но статус остаётся `DRAFT` — не отражает реальную проверенность.
- Ссылка на несуществующий `Skill System/skills/conversion-method/SKILL.md` в шаге 5 стратегии.
- Два открытых вопроса в `OPEN_QUESTIONS.md` без движения — включая «критерий успеха первого
  использования», который формально требуется до первого применения, а применение уже было.
- Инженерный жаргон в `HOME.md`/`SOUL.md`, нарушающий собственное правило «термин объясняется».
- Мелкая нумерация пунктов в `SOUL.md` (5б перед 5) — след правки без переструктурирования.
- `instructions/direct-response-policy.md` упомянут в `HOME.md`, но не найден при аудите — не
  подтверждено, ошибка это или пропуск аудита.
- `reviewer_calibration_log.md` существует, но нигде не упоминается как читаемый файл.

### TRASH
- Параллельно существующий устаревший монолитный knowledge-файл (см. WARNING выше).
- Пустая папка-заглушка `knowledge/evidence/`.
- Рабочий файл по несвязанному бизнесу («бухгалтерия/фрилансеры»), не отражённому в `PROFILE.md`.

## 4. Итоговый вердикт

**ACTION REQUIRED.**

Архитектурно агент спроектирован хорошо: контракт способностей чёткий, тон живой, дисциплина
маркеров работает, реальные провалы превращены в правила памяти, `telegram-research` — образцовый
пример документированного инструмента, Reviewer реализован технически корректно.

Три вещи — до следующей рабочей сессии:
1. Поднять запрет на числа без источника в `HOME.md` отдельным явным пунктом.
2. Разрешить конфликт `settings.json`/`settings.local.json` по `cian.ru` — одна норма, один
   файл-приоритет.
3. Обновить `ROUTING.md` — добавить строки под Avito/застройщиков/TOV, иначе новая сессия не
   найдёт материалы среди 15+ уже накопленных knowledge-файлов.
