# Готовность системы ДОМ / Skill System / Researcher / Engineer — 2026-08-12

Прямая проверка, не пересказ по памяти. Каждый пункт — FACT с источником, не впечатление.

## ДОМ (Foundation, github.com/megapolisagent/FOUNDATION)

**FACT.** Апстрим `update.sh` (проверено прямым `curl` на `raw.githubusercontent.com`, 2026-08-12) до сих пор содержит баг, найденный и исправленный локально у Engineer в тот же день: `SLUZHEBNYE_PAPKI=("skills" "instructions")` и `SLUZHEBNYE_FAYLY=("HOME.md" "AGENTS.md" "ROUTING.md" "update.sh")` — слепой `cp -R`/`cp` по этим спискам стирает точки расширения специализации (донастройку `installer`, роль в `HOME.md`, добавленные строки `ROUTING.md`) при каждом обновлении. Фикс существует только локально в ENGINEER, никогда не отправлялся апстрим — осознанное решение того дня («риск выдумать неработающую конфигурацию хуже честно оставленного разрыва» не про это, а про `.mcp.json`; здесь причина — сам update.sh не должен обновлять себя, пока апстрим не несёт то же исправление, иначе следующий запуск вернёт старый список).
**Вывод**: ДОМ как общий источник — не готов раздавать себя безопасно новым специализациям без ручного патча после каждого клона.

## Skill System (github.com/megapolisagent/SKILL-SYSTEM)

**FACT**, подтверждено повторно тем же `git status`, что и в начале сегодняшнего разговора — ничего не изменилось, потому что решение было не трогать:
- `registry.json`, `skills/decision-documentation/evidence.json`, `skills/repository-design/evidence.json` — изменены локально, не закоммичены.
- `skills/reverse-engineering/` — полностью untracked, никогда не был закоммичен (значит, отсутствует и в канонической истории, и в опубликованном `index.html`/`registry.json`).
- Ранее найденный дрейф канона `repository-design` (73 строки на складе vs 122 у ENGINEER на момент того чтения) не проверялся повторно сегодня — вероятно, разошёлся ещё больше, т.к. `repository-design/SKILL.md` в ENGINEER не редактировался сегодня, но и канон не обновлялся.
**Вывод**: механизм регистрации/фиксации реально ломается на практике — 3 находки, не 1, и они остаются открытыми по явному решению («сначала продукт, потом система»).

## Researcher (AI Intelligence)

**FACT, прямая проверка 2026-08-12**: `git status` в директории AI Intelligence → `fatal: not a git repository`. Версионного контроля нет **вообще**, впервые подтверждено прямой командой, не выводом по памяти. Весь сегодняшний объём реальной работы Researcher (аудит capability-map Engineer, исследование observability, исследование model routing, найденные и исправленные самой Researcher методологические неточности) существует только как файлы на диске — ни бэкапа, ни истории версий, ни возможности откатиться.
**Вывод**: самый серьёзный из четырёх пробелов по риску потери данных — не гипотетический, реальный объём ценной работы под угрозой без единого коммита.

## Engineer (ENGINEER)

**FACT, `git status` на момент этой записи**: не закоммичено — `DECISIONS.md`, `HOME.md`, `LIFECYCLE.md`, `MEMORY.md`, `OPEN_QUESTIONS.md`, `ROUTING.md`, `COMMUNICATION.md`, `skills/tool-selection/SKILL.md`, `skills/installer/SKILL.md` (изменены); `TECHNICAL_PASSPORT.md`, `knowledge/cost-observations.md`, 6 новых Skills (`architecture-review`, `capability-creation-methodology`, `skill-authoring`, `systematic-debugging`, `test-driven-development`, `verification-before-completion`), `workspace/agent-runs/`, `.claude/` — untracked. Весь сегодняшний Technical Passport / M1 / capability-аудит цикл нигде не отправлен на GitHub.
**Вывод**: тот же класс риска, что у Researcher, только окно короче (один день, не вся история).

## Общий вывод — калиброванно, не завышено

Система **не готова к работе** в смысле «можно доверять и не проверять» — по четырём независимым, реальным, не гипотетическим причинам, каждая уже подтверждена прямой командой, а не выводом по памяти:
1. Общий источник (ДОМ) раздаёт специализациям баг, который стирает их же настройки.
2. Общая библиотека (Skill System) не отражает свою же реальную историю использования.
3. Researcher физически не защищена от потери работы — ни одного коммита.
4. Engineer сама сейчас в том же положении — день работы не сохранён вовне.

Все четыре — устранимы быстро (в основном git-дисциплина, не архитектурные переделки), но не устранены сами по себе фактом существования системы.
