# Delegation Ledger

Реальный, построчный лог каждого запуска другого агента Инженером (`Agent`, headless
`claude -p`, `SendMessage`) — не декларация, а факт: кого вызвали, с каким брифом и
критерием, что получили, каким вердиктом закрыли. Закрывает часть Capability 7
(`dispatching-parallel-agents`/`executing-plans`/`subagent-driven-development`,
`DECISIONS_ARCHIVE.md`, 2026-08-12) — общее место учёта для делегированных вызовов, не
слияние трёх скиллов в один файл и не автоматический review-loop (это остаётся открытым,
см. `DECISIONS.md`, 2026-09-04).

## Формат записи

```
### {дата} — {краткое название}

Цель: {task_id, если создавался, иначе способ вызова}
Целевой агент: {кто}
Бриф: {дословный текст или ссылка на файл с ним}
Критерий достаточности (зафиксирован до вызова): {что считать ответом}
Результат: {факт — коммит/файл/вывод}
Вердикт: OK / FLAG — {почему}
```

## Журнал

### 2026-09-04 — Легальные каналы рыночных данных Avito (Researcher)

Цель: headless `claude -p` из директории Researcher (`AI Intelligence`) — без предварительно
созданного `task_id`; сессия не была открыта для `SendMessage` на момент постановки вопроса
(честно зафиксированный технический пропуск — брифинг ушёл через файл `OPEN_QUESTIONS.md`,
не через прямой вызов с фиксацией `task_id` до старта, как того требует
`instructions/pre-task-check.md` этого дома).
Целевой агент: Researcher (AI Intelligence)
Бриф: `AI Intelligence/OPEN_QUESTIONS.md`, запись «2026-09-04 — Легальные способы получить
рыночные/конкурентные данные Avito для AI Marketing Strategist (не самостоятельный
скрейпинг)» — 4 конкретных пункта (официальный продукт Avito / лицензированные поставщики /
технический тест единичного запроса / метод реальных компаний).
Критерий достаточности (зафиксирован до вызова, дословно из брифа): «по каждому из 4 пунктов
— конкретный найденный факт с источником, включая честное "не найдено"... Итоговый вывод —
не "можно/нельзя" одним словом, а таблица вариантов с ценой и реальным риском каждого».
Результат: все 4 пункта закрыты фактами (официальная «Аналитика спроса» ~4000₽/мес;
bnMAP.pro/наш.дом.рф/IRN.RU/Метриум как лицензированные поставщики; реальный воспроизведённый
`curl`-тест антибот-защиты Avito — 429+капча без заголовков, 200 с полными заголовками;
IRN.RU/Метриум честно раскрывают ручной, не автоматический сбор). Записано в
`AI Intelligence/knowledge/evidence/2026-09-04-avito-market-data-legal-channels-vs-scraping.md`,
коммит `26dcdf4` (уже запушен). Отдельно честно зафиксирован попутный технический сбой —
`mcp__exa__*`/`mcp__firecrawl__*` не работали весь сеанс Researcher (401).
Вердикт: OK — результат покрывает критерий по всем 4 пунктам, включая честные «не найдено»
там, где не нашлось (партнёрский канал к чужим Avito-объявлениям), не выдуманные данные.

### 2026-09-04 — Telegram-сигнал по конкурентам (Prime Park, Primavera), 30 дней

Цель: `ENG-2026-09-04-TELEGRAM-001` — task_id создан ДО запуска (исправление пропуска из
записи выше).
Целевой агент: Researcher (AI Intelligence)
Бриф: реальный сигнал по конкурентам в Telegram за 30 дней по ЖК Prime Park и Primavera —
жалобы клиентов, посты брокеров/конкурентов, ценовые упоминания. Не входит: сайты
застройщиков (отдельный запуск), YouTube.
Критерий достаточности (зафиксирован до запуска, показан владельцу, получено «да»):
минимум по одному реальному каналу на каждый ЖК или честное «канал не найден» с описанием
попытки поиска; конкретные цитаты с датами, не пересказ; если каналов нет вообще — сказать
прямо, не подменять рыночными трендами общего характера.
Результат: оба ЖК — реальные официальные каналы найдены и проверены curl'ом
(`primeparkofficial`, `primavera_moscow`, не угаданы). Единственный ценовой сигнал — по
Примавере, застройщик 2026-08-25 процитировал аналитику «Калинка» (745 тыс. ₽/м²,
t.me/primavera_moscow/5581); один пост брокера (Whitewill, брокер-тур, 2026-09-03, без цены).
По Prime Park — ни цены, ни брокерского поста в 5 проверенных каналах. Жалоб клиентов не
найдено ни по одному ЖК в открытых broadcast-каналах — честно зафиксировано как GAP, не
подменено трендом: реальные закрытые чаты дольщиков существуют (`praym_park`, `primavera_sz`),
но invite-only, вне охвата метода curl+Grep. Зафиксировано: `AI Intelligence/OPEN_QUESTIONS.md`
(закрыта), `AI Intelligence/knowledge/evidence/2026-09-04-telegram-signal-prime-park-primavera.md`.
Вердикт: OK — критерий выполнен по всем пунктам (минимум канал на ЖК, цитаты с датами, честный
GAP там, где реально пусто, не выдуман контент).

### 2026-09-04 — OSS-паттерны: Telegram-мост → headless Claude Code, логирование на VPS

Цель: `ENG-2026-09-04-TGBRIDGE-001` — task_id создан до запуска.
Целевой агент: Researcher (AI Intelligence)
Бриф: реальные (не гипотетические) open-source решения для (1) Telegram-мост → headless
Claude Code/похожий CLI-агент на сервере, (2) логирование/мониторинг headless-процессов на
VPS. Не входит: аудит безопасности и план внедрения — часть Engineer, после находок.
Контекст задачи: экосистема переезжает на «тот же Claude Code, headless, на VPS, с Telegram»
вместо стороннего харнесса (Hermes отклонён владельцем явно, чтобы не терять локальную
дисциплину HOME.md/DECISIONS.md/Skill System).
Критерий достаточности (зафиксирован до запуска, показан владельцу, получено «да»): минимум
2 реальных проекта/паттерна с прямой ссылкой на код на каждый из двух пунктов; для моста —
как вызывается CLI (webhook/polling, передача промпта, возврат ответа), модель хранения
состояния сессии, наличие аутентификации от чужих сообщений. Честное «живого репо не нашлось,
только описание паттерна» — засчитывается, не повод придумывать.
Результат: 2 реальных проекта на каждый пункт, оба напрямую проверены (README/package.json
прочитаны, не пересказаны): `RichardAtCT/claude-code-telegram`, `Mark-Life/telegram-claude-codex`
(мост); тот же Mark-Life + gist KrasimirKralev (VPS-логирование). Честно отклонено:
`madviking/headless-pm` (слово «supervisor» — название роли, не процесс-менеджер),
amux.io-страница (только сниппет, прямое чтение — 403). Зафиксировано:
`AI Intelligence/OPEN_QUESTIONS.md` (закрыта), `AI Intelligence/knowledge/evidence/
2026-09-04-telegram-headless-claude-code-bridge-vps-logging.md`.
Вердикт: OK — 2+2 реальных паттерна с кодом, честные отказы не засчитанных источников,
критерий выполнен полностью.
